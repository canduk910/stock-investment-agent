# stock-investment-agent

개인 투자자용 금융 분석 AI Agent (연세대 과제, WEEK 06~10). 원본 스펙: `invest_develop_PLAN.md` — 모든 구현 판단의 최종 기준.

## 하네스: 투자 에이전트 개발

**목표:** `invest_develop_PLAN.md`의 5주 로드맵(데이터 파이프라인 → 매크로 엔진 → 종목 리포트 → 챗봇 → 워치리스트)을 에이전트 팀으로 구현한다.

**트리거:** 개발 진행·구현·주차(WEEK) 작업·모듈 개발(매크로/종목/챗봇/프론트/데이터)·버그 픽스·QA 검증 요청 시 `invest-dev` 스킬을 사용하라. 단순 질문(코드 설명, 스펙 문의)은 직접 응답 가능.

**핵심 안전 원칙 (모든 세션에서 유지):** 매매 주문 API 절대 구현·호출 금지(조회만), LLM은 설명만(판정은 코드), 임계값 3중 일관성, 현재가 캐시 금지, 모든 구현은 TDD(테스트 먼저 — `tdd-workflow` 스킬). KIS API 코드는 kis-code-assistant MCP로 검증된 코드를 먼저 검색한다.

**UI 디자인 톤:** 모든 화면은 **흰색/회색/파랑/남색/검정 + 강조 주황(`--c-emph`) + 위험 빨강 경보(`--c-danger`)** 팔레트로 통일한다(초록·황색 배제). **가격/손익 방향은 전 화면 한국 관습(사용자 확정)**: 모든 주식 수치 상승·수익=빨강(`--c-up`)/하락·손실=파랑(`--c-down`)/보합=회색(`--c-flat`), ▲▼─ 글리프 병기(이제 차트만의 예외 아님 — `--c-chart-*`는 klinecharts 주입 경로 보존용 동일값 토큰). **주황=강조**(국면명·현금비중·목표가 상태·진입 검토·알림·확인 CTA), **빨강 경보=위험**(손실경고·VIX 패닉만, 채움형 배너/칩+⚠로 상승 빨강과 형태 구분). 난색 두 색은 역할을 섞지 않고, 가격 방향엔 금지. 색은 `frontend/src/theme.css` 토큰(`var(--c-...)`)이 단일 출처 — 하드코딩 금지. 상세는 `ui-design-system` 스킬. UI 작업 시 frontend-engineer가 이 스킬을 먼저 읽는다.

## 현황 (WEEK 10 완료 — 5주 로드맵 + 코드리뷰 백로그 + UX 개편 + UX 리디자인 "Refined Pro" + 강의요소 통합[YouTube·리포트 RAG])

데이터 파이프라인 · 매크로 2축 판정 대시보드 · 종목 종합리포트 · **LLM 챗봇**(agent 루프·ML 인텐트 6분류·서버 세션·SSE 스트리밍) · **워치리스트(모듈 3)**(CRUD + 능동 목표가 알림) · **[P2] 구조화 리포트**(`StockReport` Pydantic 검증·히스토리) 동작 — **5주 로드맵(WEEK 06~10) 완성**. 이후 **코드리뷰 백로그 20건(IMP-01~20)** 반영 + **UX 개편**(좌측 상시 채팅 + **우측 동적 패널** — 팝업 모달 폐기, 챗 tool_call·퀵버튼 병행 구동 + **잔고(포트폴리오) 패널** 신규 `GET /api/balance`, 조회 전용·무캐시). 이후 **UX 리디자인 "Refined Pro"**(디케이 브랜딩[DK 모노그램·파랑 워드마크]·Pretendard·tabular-nums·**가격/손익 방향색 반전**[상승·수익=빨강/하락·손실=파랑을 **전 화면** 적용, 차트만의 예외 → 전역]·세그먼트 탭·**카드 로우 관심종목**[등락 칩·목표가 근접 게이지·**스파크라인** watchlist `spark` 필드]·**네이비 잔고 히어로 카드**·네이비 현금비중 카드·마이크로모션·전환 스켈레톤). 이후 **W09 강의 통합**(챗 **콘텐츠 툴** 메커니즘 — 표시 툴과 구분해 서버가 실행·실텍스트 되먹임: **YouTube 자막 요약**·**증권사 리포트 PDF RAG**[`rag/` numpy 코사인]) + **KIS 좀비 토큰 자가치유**(5xx 본문 표면화·EGW00121/123 재발급 재시도) + **네이버 애널리스트 리포트 수집·요약·챗 상담 연계**(수집→보고서별 구조화 요약[`AnalystReportSummary` 안전강제]→ticker 저장→종목 상세 섹션 + "이 리포트로 상담하기"[세션 핀 컨텍스트, 출처 귀속·판정 금지·면책]). 이후 **챗 "현재 보는 화면" 컨텍스트 연계**(잔고·관심종목·종목상세 스냅샷을 세션 핀 `Session.view_context`에 실어 대화 반영 — 표시 툴 `{ok:True}`만으로 챗봇이 화면 숫자를 몰랐던 문제 해결. `chat/view_context.py` 서버 재조회 컴팩트 스냅샷[기준시각·top-N·인용만·판정 금지·면책], **P1** 프론트 패널변경 시 `POST /api/chat/context` 핀 + **P2** 챗 툴 결과 스냅샷 되먹임으로 같은 턴 즉답. guardrail 선차단 불변). 이후 **챗 자문 완화**(위험 2층 차단[결정적 정규식 하드블록 + ML→LLM 재분류] + 코드근거 포트폴리오 상담). 이후 **유저베이스 + 시황 + 대화기록**(GCP 지향 5-Phase): ①**시황(market outlook) PDF 요약** 시장 국면 페이지 표시, ②**회원가입/로그인**(SQLAlchemy DB[로컬 SQLite→프로덕션 GCP Cloud SQL Postgres, `DATABASE_URL` 스왑]·bcrypt+JWT·`get_current_user` 게이트), ③**유저별 관심종목**(`SqlWatchlistStore` Protocol 스왑·인증 스코프·유저 격리), ④**대화기록 저장·새 대화**(Conversation/ChatMessage·챗 write-through·hydrate·대화 스위처), ⑤**애널리스트·시황 요약 공동 DB**(전역 공유 `AnalystReportRow`·`ScopedReportRepo` 스레드안전). 이후 **후속 개선 항목**: 리포트 가져오기 SSE 진행표시(항목1)·대화창 자동/수동 명명(항목2)·**국면 로직 단순화(항목3)**(국면별 종목 진입게이트[`regime_gate`·`entry_signal`·PER/PBR/편입비중 커트] 폐기 — 국면은 **현금비중만** 관리, 종목 raw PER/PBR·`valuation_label` 은 정량 데이터로 유지, 리포트 `국면정합성` 은 LLM 이 국면명+권장 현금비중 대비 '최종 적합성' 서술[`report.py::_regime_block`]·번들↔국면 디커플[GET bundle 낭비 FRED 호출 제거]). 이후 **시황 리포트 UX(항목4)**: 증권사 매크로보고서를 **일별 구분 + 컴팩트 3줄요약 카드 + 클릭 상세 오버레이**(`MarketOutlookSummary.세줄요약` 신규[min1·max3, 구 레코드는 `핵심요지[:3]` 폴백]·`lib/marketOutlook.js` 순수 그룹핑·`MarketOutlookDetailOverlay` 딤배경 Esc/✕/배경 닫힘 — 모달폐기 관습의 의도적 예외[시황 상세 전용]). 이후 **애널리스트 종합요약(항목5)**: 종목별 증권사 리포트 **최근 3개를 종합해 10줄요약**(`analyst_combined.summarize_recent_reports` — 저장된 per-report 요약만으로 LLM 1회 종합·PDF 재다운로드 0·`CombinedAnalystSummary`[종합요약 max10·의견분포·목표주가범위·면책]·`POST …/analyst-reports/summary` 온디맨드·`AnalystReportsSection` 상단 "종합요약 생성" 패널). 이후 **종목검색 자동완성 원복(항목6)**: 우측 패널 `TickerSearch`에 종목명 자동완성 복원(`searchStocks`·`GET /api/stocks/search` KIS 마스터 — **백엔드 완비, 프론트만**·`.autocomplete` 드롭다운·키보드·선택 시 `stock_report` spec + 종목명). 이후 **종목리포트 관심종목 별 토글(항목7)**: `StockReportView` 헤더에 `WatchlistStar` 위젯(자체 멤버십 조회·add/remove 토글·**★ 등록완료=주황/☆ 미등록=회색**·409 graceful·명시적 클릭만 — 백엔드 완비 워치리스트 API 재사용·프론트만). **← 5주 로드맵 후속 개선 7항목(항목1~7) 완결**. **모델은 OpenAI `gpt-5.6-luna`**(`chat/tools.py::CHAT_MODEL` 단일 출처, **추론형** → `CHAT_MODEL_PARAMS={reasoning_effort:'none'}` function tools 필수). 실행: 백엔드 `uv run uvicorn api.main:app --port 8000` + 프론트 `cd frontend && npm run dev` → `localhost:5173`, **또는 `docker compose up --build`**(루트 `DOCKER.md`; **`uv add` 후 `docker compose exec backend uv sync` 필수**). 테스트 `uv run pytest`(677개; 라이브는 `-m live`, 키 필요) + 프론트 `cd frontend && npm test`(vitest 203개). **GCP 프로덕션 배포 완료**(`DEPLOY_GCP.md`): **단일 Cloud Run 서비스**(FastAPI 가 `/api` + 빌드된 React `dist` 를 같은 오리진 서빙 → CORS·프론트 코드 변경 0, SSE 그대로) + **Cloud SQL Postgres**(`DATABASE_URL` 스왑·`psycopg`) + Secret Manager. 루트 `Dockerfile`=프로덕션(멀티스테이지·`$PORT`·no-reload), `Dockerfile.dev`=로컬 개발(compose 참조). `api/main.py` 말미가 dist 서빙+404 SPA 폴백(비-API GET 만; `/api` 405/JSON 에러 불변, dist 없으면 skip). 라이브 검증 완료(health·SPA·regime·회원가입[Cloud SQL 쓰기]·로그인·잔고[KIS]·관심종목·챗[OpenAI]). 이후 **항목1~7 프로덕션 재배포**(리비전 갱신·라이브 스모크) + **GitHub Actions CI/CD 구축**(`.github/workflows/ci-cd.yml` — main 푸시·PR 테스트[pytest+vitest+build], main 푸시 통과 시 Cloud Run 자동배포; **WIF 키리스 인증**·`vars.WIF_PROVIDER` 게이트[미설정 시 deploy skip]; WIF/IAM 설정 스크립트는 `DEPLOY_GCP.md`에 문서화·**사용자가 직접 실행**). 이후 **시황 자동 최신화**(시장 국면 패널 로드 시 저장된 최신 시황 `date`가 KST 오늘과 다르면 기존 SSE 수집을 자동 트리거·`localStorage` 날짜별 1회 가드·프론트만). 이후 **종목 기술적 분석 — 고지로 이동평균선 대순환**(종목 상세 기술적 분석 하단: 3 SMA[단기5/중기20/장기40] 배열로 6단계 국면 판정 + 밴드[단기−장기 폭·확대/축소] + 단계 지속/전환 — **결정적 엔진**[`stock/summary.py::_ma_grand_cycle`]·`GRAND_CYCLE_STAGES` 라벨 SSOT·`INDICATOR_CONFIG.grand_cycle` 로 프론트 전파·번들 자동. `GrandCyclePanel`[6단계 스텝·현재 주황 강조·밴드·인사이트·면책] + 캔들차트 3MA 오버레이[단기 주황/중기 회색/장기 남색]. 판정은 코드·서술은 프론트·면책 고정·일봉 무캐시). 이후 **권한 관리(RBAC) + 질문 사용량 한도**(관리자 제어): `User` 권한/사용량 컬럼(`is_admin`·`daily_limit`=20·`used_today`·`usage_date`·`total_questions`) + `auth/usage.py`(**질문 횟수 기반·매일 KST 리셋·관리자 무제한**·판정은 코드) + `get_admin_user`(403 게이트) + `admin_seed`(startup·`ADMIN_EMAILS` 기본 `dukkikim@yonsei.ac.kr` 기존 유저 승격). 챗 `/api/chat`·`/stream` 진입부 `is_over_limit` 선차단(LLM 미호출) + 턴당 `consume`. 관리자 API `api/admin.py`(GET users[통계·해시/KIS원문 미노출]·PATCH[권한·한도 부분갱신]·reset-usage·DELETE[유저+스코프데이터 정리]·**자기 관리자해제/삭제 400**). 프론트 `AdminPanel`(우측 '회원 관리' 탭·관리자만) + 톱바 질문 잔량 칩(관리자 무제한/일반 N/M). 라이브 e2e 통과. 이후 **국면 이동 궤적(족적) 시각화**(시장 국면 대시보드 강화): 시장 국면 2×2 매트릭스 **바로 아래**에 최근 N개월(12/24/36 선택·기본 36) 국면 이동을 **같은 매트릭스 위 트레일**로 표시 — **엔진이 순수·결정적이라 과거 월별 지표를 `judge_regime` 에 재현**(`macro/regime_history.build_trajectory`·LLM 미개입)·`GET /api/macro/regime/history`(FRED 3+공포탐욕 병렬 수집·확정 과거값 캐시·**진행 중 당월 결정적 제외**[FRED 부분월 평균 아티팩트 방지]·양축 결측 스킵·graceful). 프론트 `RegimeTrajectory`(자체 완결 SVG·과거 회색→현재 주황·전환 화살표·기간 탭·면책)+`lib/regimeTrajectory`(**`regimeMarkerPos` 좌표 SSOT** — 게이지 마커와 공유·셀 겹침 황금각 오프셋). 적대적 다각검증(정확성·계약·안전·UX)으로 당월 부분데이터 아티팩트 발견·수정. 백엔드 855+프론트 338 green, 라이브 실증(36개월 2023~2026·역수익률곡선기 정합). 이후 **종목 차트 대순환 스테이지 리본**(기술적 분석 강화): 종목 상세 캔들차트 하단에 최근 국면이 **언제 어느 대순환 단계(6단계)였는지** 얇은 띠로 표시(5/20/40 MA 는 기존에 이미 렌더). **엔진 재사용**: `_stage_at`(순수 분류)로 봉별 단계 → `_grand_cycle_segments`(연속 런 그룹핑·날짜 키)를 `ma_grand_cycle.stage_segments` 로 노출(판정=코드·LLM 0). 프론트 klinecharts 커스텀 오버레이(`registerOverlay`/`createPointFigures`)로 캔들 좌표 정렬 리본(구간별 rect+번호+글리프 ▲▼◆·현재 주황·뉴트럴 국면 톤·방향색 금지). 적대적 4렌즈 검증(klinecharts API 포함) 무결함. 백엔드 860+프론트 345 green, 라이브 실증(005930 6구간·마지막=현재 4단계 역배열 정합). 이후 **종목 차트 강화 — 일봉/주봉 × 3개월/1년/3년/10년 선택**: 고정 일봉·6개월이던 차트를 **주기(일봉/주봉)×기간(3개월/1년/3년/10년) 모든 조합 선택형**으로. KIS `FHKST03010100` 는 period 로 주/월/년 이미 지원(API 변경 0)·**~100/콜 상한은 date-window 후진 페이지네이션**(`chart.fetch_chart_series`)으로 극복. 신규 `GET /api/detail/{ticker}/chart?period=&range=`(표시 데이터만·`stage_segments_for_chart` 로 리본 표시 시계열 재계산·일봉 무캐시·graceful). 프론트 `ChartSection`(세그먼트 선택기+`useFetch` 스왑·번들 차트 fallback)·`api.fetchStockChart`. **정량 요약·GrandCyclePanel 은 번들[일봉]에 pin**(차트 탐색이 판정을 안 바꿈). 적대적 4렌즈 검증(페이지네이션·계약·안전+pin·행동보존) 무결함. 백엔드 871+프론트 350 green, 라이브 실증(D/3m 60·D/1y 243·D/3y 728·W/10y 521·D/10y 2452[~25콜 2.5s]).

## 디렉토리 문서 지도

기능별 세부 지식(결정·함정·계약)은 각 디렉토리 `CLAUDE.md`에 있다(해당 디렉토리 작업 시 자동 로드):
- `collectors/CLAUDE.md` — KIS 어댑터(오류 표면화·토큰 backoff·라이브 확정 필드명)·지표 수집기 계약
- `cache/CLAUDE.md` — 캐시 3원칙의 구조적 강제 + 클라우드 전환 계약
- `api/CLAUDE.md` — FastAPI = Lambda 로컬 스탠드인, 엔드포인트 계약(매크로·번들·챗/SSE·워치리스트 CRUD·구조화 리포트·잔고 조회)
- `auth/CLAUDE.md` — 인증(회원가입/로그인): bcrypt 해시·JWT·`get_current_user`·GCP 이식성
- `watchlist/CLAUDE.md` — 관심종목 백엔드: 시세·목표가·스파크라인(국면 진입게이트 폐기·항목3)·**SqlWatchlistStore(인증 스코프·`(user_id,ticker)`)**·상한 게이트(409)·목표가 매수관점 semantics
- `chat/CLAUDE.md` — LLM 계층: CHAT_MODEL 단일·가드레일 코드결정·ML 인텐트·기준표 자동생성·서버 세션·SSE 스트리밍(tool_calls 재조립)
- `frontend/CLAUDE.md` — 실행법(IPv6 localhost·도커)·디자인 토큰·API 계약·챗봇 SSE 스트리밍 UI·**UX 레이아웃(좌측 채팅+우측 동적 패널, 모달 폐기)**·잔고 패널
- 루트 `DOCKER.md` — 로컬 도커 기동(백엔드+프론트 2컨테이너, .env 런타임 주입, 핫리로드)
- 루트 `DEPLOY_GCP.md` — GCP 프로덕션 배포(단일 Cloud Run[API+정적] + Cloud SQL Postgres + Secret Manager, 아키텍처·리소스·재배포·비용/정리) + **CI/CD(GitHub Actions + WIF, main 자동배포·WIF 설정 스크립트)**

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-24 | **YouTube 자막 추출 실패 사유 표면화(진단 가능) — IP 차단 vs 자막 없음 구분**. 사용자가 배포 사이트에서 유튜브 링크 자막 추출 실패 신고 → 진단: **로컬은 정상**(라이브러리 1.2.4·사용자 준 영상 `uv1DAiV49gs` 자막 10,565자 추출), **프로덕션(Cloud Run)만 실패** = **YouTube 가 데이터센터 IP 차단**(`RequestBlocked`/`IpBlocked`/`PoTokenRequired`) 확정 — youtube-transcript-api 의 대표 클라우드 제약(버그 아님). **추가 문제**: 기존 `except Exception: return None` 이 모든 실패를 뭉개 "자막 못 가져옴"만 안내(원인 불명). 사용자 결정(AskUserQuestion)=**명확한 안내 메시지만**(프록시 없이). **구현**: `collectors/youtube.fetch_transcript_detailed(url)→(text, reason)` 가 예외를 isinstance 로 분류(버전 방어 `_exc()`)해 사유 반환 — **REASON_BLOCKED**(IP 차단·"로컬은 정상"), **REASON_NO_CAPTION**(자막 없음), REASON_UNAVAILABLE/TIMEOUT/BAD_URL/GENERIC. `_impl_summarize_youtube` 가 사유를 LLM 에 되먹여 사용자에게 정확히 안내 + WARNING 로그에 예외 타입(프로덕션 Cloud Run 로그 진단). `fetch_transcript`(str|None)는 하위호환 래퍼. **안전**: 조회 전용·매매 API 0·타임아웃 상한 불변. TDD(youtube +6: 분류·차단사유·자막없음사유·성공·불량URL·빈결과 / chat·tools mock 갱신). 백엔드 944→**950 green**. 통합 실증(차단 시뮬→콘텐츠 툴이 IP차단 사유 반환·실 영상 정상 자막). **미해결(설계상)**: 프로덕션에서 실제 자막을 가져오려면 residential 프록시 필요(사용자가 messaging-only 선택) | collectors/youtube·chat/(tools·CLAUDE) + 테스트 | 사용자: 배포 사이트에서 유튜브 자막 추출 안 됨(왜?) |
| 2026-07-24 | **종목 상세의 '생성된 AI 리포트'를 챗 종목문의 시 자동 참조**. 사용자가 종목 상세에서 'AI 리포트 생성'·애널리스트 보고서 수집을 해 두면, 그 종목을 챗에서 물을 때 자동으로 가져오게. **조사 확정**: 애널리스트는 이미 `view_context._stock_context` 가 top-2 를 핀했으나(부분 연동) **AI 구조화 리포트(`report_store`)는 챗에 전혀 미참조**가 핵심 갭. 사용자 결정(AskUserQuestion): **AI 리포트 추가 + 애널리스트는 top-2 유지**(변경 최소·프롬프트 예산). **구현(기존 view_context 배선 재사용·백엔드만)**: `view_context._latest_ai_report(ticker)`(순수 JSON 읽기·LLM 0·예외 없음) 로 `report_store.list_history(ticker)[0]` 최신 `StockReport`(종합의견·요약·투자포인트·리스크요인·국면정합성)를 `_stock_context` 스냅샷에 추가(미생성이면 블록 생략). **트리거**: 종목 상세 패널이 열린 상태(리포트 생성 직후)면 P1 이 그 종목을 이미 핀 → 챗 문의 시 자동 딸려옴(P2 show_stock_report 도 동일). **안전**: 종합의견 Literal(긍정/중립/신중)=매수·매도 판정 아님(인라인 명시)·면책 불변·1500자 예산 내·매매 API 0·판정=코드. TDD(view_context +3: AI 리포트 포함·부분키 graceful·미생성 생략, 기존 test hermetic 화). 백엔드 941→**944 green**. **통합 실증**: report_store 실 저장→`build_view_context` 가 AI 리포트 블록 포함 확인(실 파일 읽기, 모킹 아님). 챗 참조 경로는 직전 intent e2e("클래시스 44,250원" view_context 인용)로 기실증 | chat/(view_context·CLAUDE)·tests/unit/chat/test_view_context | 사용자: 종목상세 AI리포트·애널리스트 생성 후 챗 종목문의 시 가져오기 |
| 2026-07-24 | **프론트 후속 2건 — 플래키 테스트 견고화 + 좌/우 패널 크기 조절(드래그·전체폭)**. ①**플래키 테스트**(intent 커밋 CI 첫 실행에서 실패→재실행 통과했던 `AdminPanel.test.jsx` 한도 편집): 근본원인 = 저장 버튼이 `disabled={!limitChanged}` 라 `fireEvent.change`→`fireEvent.click` 사이 controlled input 상태가 CI 타이밍에서 재렌더로 flush 되기 전 클릭 시 **disabled 버튼 onClick 미발화 → updateAdminUser 0회**(RTL act() 이슈). 컴포넌트는 정상(변경 전 저장 비활성=올바른 UX) → **테스트만** 견고화: `await waitFor(버튼 enabled)` 후 **매번 새로 쿼리해 클릭**(로컬 5/5 통과). ②**좌/우 패널 크기 조절**(사용자 결정=화면 전체폭+드래그): `.app` max-width 캡(1360px) **제거**→화면 전체폭, `.app__main` 그리드 `var(--chat-w,420px) 14px minmax(0,1fr)` + **`.app__divider`(role=separator·드래그/방향키/더블클릭리셋)**. 순수 `lib/panelLayout.js`(`clampChatWidth`=**채팅 하한 320·우측 최소폭 420 보장**·`load/saveChatWidth` localStorage 영속·graceful). ≤1024px 세로스택은 분할선 숨김. **색 토큰만**(회색 라인→hover/드래그 주황 `--c-emph`=인터랙티브, 방향/경보색 아님·hex 0). **브라우저 라이브 검증**: 로그인→드래그 420→756→320(하한)→940(상한)→600, 새로고침 후 600 복원·우측 패널 항상 ≥420px. TDD: `panelLayout.test.js`(순수 9)+`App.test.jsx`(separator·방향키 `--chat-w` 증감). 프론트 384→**394 green**, build clean, 안전 grep(hex·방향/경보색 오용 0). **백엔드 변경 0** | frontend/(App·styles·lib/panelLayout[신규]·AdminPanel.test·App.test·CLAUDE) + 테스트 | 사용자: 플래키 테스트 견고화 + 좌/우 패널 크기 자유 조절(최대폭 포함) |
| 2026-07-23 | **intent 분류기 고도화 — 비유명 종목 macro_view 오분류 수정(gazetteer override + 데이터 균형)**. 챗에서 유명주("삼성전자 어때?")는 종목리포트로 가는데 **비유명 KRX주**("롯데렌탈/에스피지/클래시스 어때?")가 시장 국면(macro_view) 패널로 새서 종목 정보에 접근 못 하던 버그. 원인(3-agent 정량 조사) = 학습데이터 유명주 편중(43:8) + 바로 "{종목명} 어때?" 구조가 stock_analysis 에 전무 → char n-gram 이 짧은 "어때?"를 macro 신호로 학습 + `merge_intent_panel` 이 macro 패널을 `popups[0]`에 주입해 LLM 의 종목리포트를 덮음. **사용자 결정(이전 세션 승인 계획)**: Layer A(gazetteer) + Layer B(데이터 보강) 둘 다·len≥3만 인식. **Layer A(결정적 pre-gate)**: 신규 `chat/stock_gazetteer.py::query_names_a_stock` — KRX 마스터(`collectors.stock_master.load_stock_master`·`.cache` 재사용·in-process 캐시)의 개별 종목명(len≥3·ETF/인덱스/인버스 제외·긴이름 우선)이 **질문 부분문자열(name⊆query)**이면 반환. `classify()`가 ML 예측 뒤 **`label=="macro_view"` 일 때만** 매치 시 `stock_analysis`로 재분류(가드레일 선차단 뒤·gazetteer 예외 graceful). **override 타깃 stock_analysis**: `INTENT_PANEL` 미매핑이라 `merge_intent_panel`이 popups 를 안 건드려 LLM `show_stock_report`(ticker) 생존(최소·안전). **Layer B**: `data/intent_dataset.tsv` +53행 재학습(오프라인). **★적대적 4렌즈 검증(정확성·오탐·안전·계약)이 진짜 회귀 3건 포착**: (i,ii) stock "어때?" 보강이 짧은 조회형을 stock 으로 끌어당겨 **매크로(FX/원자재/통화·환율/유가/달러/인플레/엔화/위안화/리세션)·포트폴리오 질문이 stock 으로 새는 역방향 회귀**(결정적 패널 라우팅 상실) — **영향 라벨 전부에 동일 짧은형 보강**(macro 지수/시장+FX/원자재/금리/인플레+통화/외래어/해외지수, portfolio 짧은형)으로 해소, (iii) intent.py docstring 6분류→7분류 갱신. **아키텍처**: gazetteer=KRX len≥3 **결정적 보장**·데이터 균형=macro·portfolio **누수 방지**·외국주/2글자주는 best-effort(baseline 동일·회귀 아님). **안전**: 매매 API 0·판정=순수 문자열(LLM/확률/랜덤 0)·guardrail 선차단 불변·override 는 macro_view 예측일 때만(종목명 든 watchlist/portfolio 오발 0). TDD(Red→Green): 백엔드 941 green(+gazetteer 10·override 유닛·`test_krx_stock_queries_route_to_stock_analysis`[classify 통합]·`test_macro/portfolio_queries_do_not_leak_to_stock_analysis`[회귀 잠금]) + 프론트 384 green(무영향). baseline 대조 macro→stock flip 전수 0 실증 | chat/(stock_gazetteer[신규]·intent·CLAUDE)·data/intent_dataset·chat/models/intent_clf.joblib·tests/unit/chat/(test_stock_gazetteer[신규]·test_intent) | 사용자: 비유명 종목이 종목정보 대신 시장국면 패널로 감 |
| 2026-07-21 | **프로덕션 버그 2건 수정 — 챗 잔고 자문 복구 + 관심종목 추가 확인 카드 복구**. ①**잔고**("내 잔고 보여줘"가 챗에서만 "일시 조회 불가", 패널은 정상): 근본원인 = 챗 P2 되먹임(`_display_tool_result`)이 `build_view_context`에 `user`/`db`를 안 넘겨 `resolve_kis_client`가 **DB(`__shared__`/유저) KIS 키를 못 읽고 env fallback**만 씀 → **프로덕션은 KIS 앱키를 env(Secret Manager)에서 제거하고 암호화 DB에만 seed**해 실패(패널/P1은 db 전달로 정상, 로컬은 `.env`에 키 있어 안 드러남). 수정 = `api/chat.py`→`chat()`/`chat_stream()`→`_display_tool_result`→`build_view_context`로 **user/db 관통**(패널과 동일 경로). ②**관심종목 추가**(챗은 "확인 눌러 등록" 안내하나 우측 확인 카드 안 뜸): 근본원인 = 이번 세션 인텐트→패널 라우팅 회귀 — `watchlist_mgmt` 인텐트가 `show_watchlist`(목록)를 `merge_intent_panel`로 `popups[0]`에 밀어넣어 LLM이 낸 `manage_watchlist`(확인 카드)를 가림. 수정 = `merge_intent_panel`에 **`_ACTION_PANELS`(=manage_watchlist) 예외** — 편집 확인 write 카드가 있으면 인텐트 네비 패널 미주입(확인 카드 popups[0] 유지·조회 패널 충돌 시 인텐트 우선은 유지). **안전**: 매매 API 0·판정 코드 불변·guardrail 선차단 불변·프론트 변경 0(두 건 다 백엔드). TDD(잔고 관통 유닛+통합, 확인카드 예외 2). 백엔드 922 green. **적대적 진단**(로컬 정상인데 프로덕션만 실패 = env vs DB 자격증명 비대칭 규명, 재현으로 확정) | chat/(chat·intent_panel·view_context 경유·CLAUDE)·api/chat·CLAUDE + 테스트 | 사용자: 챗 잔고 자문 불가 + 관심종목 추가 확인 카드 무반응 |
| 2026-07-21 | **시장국면 대시보드 재구성 — 최상단 '금일의 요약'(자동) + 챗 답변에 최신 시황 반영**. 사용자 요청 3부: ①금일의 요약을 대시보드 **최상단**으로(기존 시황 섹션 안 수동 버튼 → 상단 자동 카드), ②`[네이버 최신 시황 가져오기 → 요약 생성]`이 **차례로 자동 실행**된 뒤 생성, ③챗 답변도 **최신 시황을 국면 판정과 함께** 컨텍스트 반영. 사용자 결정(AskUserQuestion): 챗 시황=**최근 요약 3개 직접 주입**(추가 LLM 0·macro_view 게이팅), 자동 생성=**하루 1회**. **Part A/B(프론트)**: 시황 lifecycle 을 **신규 컨테이너 `MacroDashboard.jsx`로 리프트**(RightPanel 이 macro_dashboard 에서 렌더) — 레이아웃 `[금일의 요약] → [RegimeGauge] → [증권사 시황 카드]`, 마운트 `load()`→stale 이면 `fetchNaver()`(SSE)→완료 후 재조회→`maybeAutoSummary(force)` 순차, 최신이면 곧바로 요약(하루 캐시 `mo_daily_summary` 재사용/1회). `DailySummary.jsx`(추출·controlled 표시형)·`MarketOutlookSection.jsx`(자체fetch·자동수집·지역DailySummary 제거 → **controlled 카드 뷰**, props 수용·상세오버레이/그룹/3줄폴백/진행바 보존). **Part C(백엔드)**: `market_outlook.build_recent_outlook_context(limit=3)`(저장 요약 3개 조합·추가 LLM 0·graceful None)를 `chat.py`가 `intent=="macro_view"`일 때만 `_build_system_prompt(judgement, session, outlook_context)`로 **per-turn 주입**(핀 아님·report_context 무충돌·`_OUTLOOK_CONTEXT_HEADER` 출처귀속·판정 금지·면책, guardrail 선차단 불변). **적대적 4렌즈 검증(계약·정확성·안전·UX)** → 확정 1건(low UX: 백엔드 장애+첫 마운트 시 진행바+에러 동시표시) 수정 = `load()` 에러 시 `null` 반환(빈배열 []과 구분)·load 실패면 자동 최신화 스킵. TDD: 프론트 384(+MacroDashboard 7·DailySummary 5·MarketOutlookSection 재작성 9)·백엔드 918 green, build clean, 안전 grep 0(주문 API·방향/경보색·hex) | frontend/(MacroDashboard[신규]·DailySummary[신규]·MarketOutlookSection·RightPanel·styles·CLAUDE)·chat/(market_outlook·chat·CLAUDE) + 테스트 | 사용자: 대시보드 최상단 금일의 요약(자동) + 챗 최신 시황 RAG |
| 2026-07-21 | **인텐트 분류기 → 우측 패널 결정적 라우팅(최초 설계 반영)**. 조사로 확정: 원 설계(인텐트 분류→알맞은 패널 자동전환)와 달리, 실제 패널 전환은 **LLM function calling**이 결정하고 인텐트 분류기는 **위험 가드레일 차단**에만 쓰였다(intent→panel 매핑 0). 사용자 결정(AskUserQuestion): **인텐트가 패널을 직접 선택하는 결정적 경로 추가**. 신규 `chat/intent_panel.py`(순수 SSOT): **인자 없는 3개 네비게이션 라벨만** 매핑(`macro_view→show_macro_dashboard`·`watchlist_mgmt→show_watchlist`·`portfolio_advice→show_balance`) + `merge_intent_panel(intent, popups)`(인텐트 패널을 popups **맨 앞에 주입**+LLM 중복 dedup). `chat/chat.py`의 `chat()`·`chat_stream()`이 `intent=classify()` **1회** 계산 후 병합(폴백에도 적용, `chat_stream`은 도구 호출 없어도 popups 이벤트 emit). 프론트가 `popups[0]`로 전환하므로 **인텐트가 패널을 권위적으로 결정** — ticker 필요한 종목리포트·args 필요한 편집은 LLM 병행(매핑 0). **안전**: 편집·매매 패널 매핑 0(오분류가 편집/주문 유발 0)·가드레일 2층 선차단 불변·팝업 계약·`popupRouter` SSOT 재사용 → **프론트 변경 0**·view-context 되먹임 보존. TDD(intent_panel 7·chat 4·stream 1)·백엔드 911 green. 노트북에 intent→패널 데모 셀 추가(재실행). **적대적 검증 예정** | chat/(intent_panel[신규]·chat·CLAUDE)·CLAUDE·README·notebooks + 테스트 | 사용자: 원 설계(인텐트→패널) 반영 확인/구현 |
| 2026-07-20 | **시황(매크로) 리포트 챗 상담 + '금일의 요약' 종합 카드**. 애널리스트 리포트는 report-context 로 챗 상담·최근3 종합요약이 됐지만 시황은 불가였음 → **애널리스트 패턴 미러**. ①**개별 상담**: `market_outlook_store.get(report_id)`·`market_outlook.format_market_outlook_context`(시장 전체라 종목·목표주가 없음·시장전망 있음)·`POST /api/chat/market-outlook-context {session_id, report_id}`(애널리스트와 **같은 세션 핀 슬롯**·서버 store 조회[프론트 신뢰전송 0]·없으면 404·해제) → 프론트 `setMarketOutlookContext`·`RightPanel→MarketOutlookSection sessionId/onConsult`·상세 오버레이 '이 시황으로 상담하기' CTA(성공 시 좌측 챗 배너+오버레이 닫힘). ②**금일의 요약**: `market_outlook_combined(_schema).py`(최근 **5개** 종합·**중복제거**→최대 10줄, `CombinedMarketOutlookSummary` 시장전망분포·종합요약[max10]·면책 필수·**report_count 코드계산**[LLM 환각 방지]·0개 LLM미호출·저장 요약만[PDF 재다운로드 0])·`POST /api/macro/market-outlook/summary`(graceful) → 프론트 `fetchMarketOutlookSummary`·`DailySummary` 카드(섹션 헤더 **바로 아래**·**주황 강조 소프트+좌측 강조선+📌**·저장 시황 있을 때만). ③**일별 자동 최신화는 기존 구현**(재확인). **안전**: 매매 API 0·시황 요약/종합=리포트 인용(시장 판정 아님)·면책·출처 복수 귀속·색 토큰(주황 강조, 방향/경보/hex 0). TDD(백엔드 context 4·combined 9·route 2, 프론트 consult 2·daily 2). **적대적 4렌즈 검증**. 백엔드 897+프론트 375 green | chat/(market_outlook·market_outlook_store·market_outlook_combined[+schema])·api/(chat·macro_outlook)·frontend/(api·RightPanel·MarketOutlookSection·styles·CLAUDE) + 테스트 | 사용자: 시황 리포트도 RAG처럼 챗 상담 + 시황 5개 종합 10줄 금일의 요약 |
| 2026-07-20 | **유저 대화기록 삭제 + 판정근거지표 히스토리 5년**. ①**대화 삭제**(프론트만·백엔드 `DELETE /api/conversations/{id}` 기존·유저 스코프): `ChatPanel` 대화 스위처에 🗑 **2단계 확인**(삭제?/✕·브라우저 dialog 금지 관습)·`App.handleDeleteConversation`(서버 삭제 성공 후 목록 제거·현재 대화 삭제 시 남은 첫 대화 전환·없으면 새 대화[최소1 보장]·실패 시 목록 유지). ②**지표 5년**: 판정근거 지표 카드 클릭 히스토리 오버레이가 1년(12개월)뿐 → **5년(60개월)**(`MacroIndicatorHistoryOverlay` `HISTORY_MONTHS=60`·문구 '최근 5년'·`MacroIndicatorCards` 라벨). 백엔드 clamp 이미 1..60·`MacroLineChart` x라벨 최대 6개 솎음(점 수 무관)이라 백엔드/차트 무변경. TDD·색 토큰(삭제 확인 CTA=주황) | frontend/(App·ChatPanel·MacroIndicatorHistoryOverlay·MacroIndicatorCards·styles·CLAUDE) + 테스트 | 사용자: 대화기록 삭제 + 지표 그래프 5년 |
| 2026-07-20 | **헤드라인 가입자수·방문자수**. 톱바 헤드라인에 **가입자수(회원 총수)+방문수(누적 총 + 오늘 KST)**. 백엔드: `infra/site_stats.py`(`SiteStat` 싱글턴 행·`record_visit`[누적 +1·오늘 KST 날짜경계 리셋]·`get_site_stats`[User count + 방문]), `api/stats.py`(`POST /api/visit` 앱로드 1회·`GET /api/stats` 공개·**PII 없음·집계만**·항상 200 graceful), `infra/db.import_models` 등록(신규 테이블 create_all). 프론트: `App` 마운트 시 방문 1회 기록(`_visitRecorded` 모듈 가드·StrictMode 방어) 후 조회, 톱바 `.app__stats`(가입자/방문·**'오늘'만 주황 강조**·tabular-nums). TDD(백엔드 5·프론트 1). 사용자 결정: 누적 총 방문 + 오늘 | infra/(site_stats·db)·api/(stats·main)·frontend/(App·api·styles·CLAUDE) + 테스트 | 사용자: 헤드라인에 가입자·방문자수 |
| 2026-07-20 | **국면 궤적 — 표본 라벨 축 pole 라벨 겹침 수정**. 1년 궤적에서 최상단 표본(경기=+2, y=12)의 월 라벨이 위로 배치돼 상단 축 라벨 **"경기 양호"**(y=5, 프레임 위 여백)와 겹쳤음 → 순수 `placeLabelY(y, up, down)`(`lib/regimeTrajectory.js`): 위/아래 절반 규칙이되 **상단(y<20)은 점 아래·하단(y>80)은 점 위로 뒤집어** pole 라벨 구역(상/하단 여백)을 피해 프레임 안쪽에 머문다. 표본 라벨·live 콜아웃·레거시 현재 라벨 3곳 공통 적용. 판정·좌표·색·면책 불변. TDD(순수 3[상단 아래·하단 위·중간 유지]·컴포넌트 1[상단 라벨 y>10]). 프론트 367 green·build clean | frontend/(lib/regimeTrajectory·RegimeTrajectory·CLAUDE) + 테스트 | 사용자: 경기 양호 라벨과 궤적 점 라벨 겹침 |
| 2026-07-20 | **국면 궤적 — 표본별 개별 점(같은 칸 반복 오프셋·라이브 defer)**. 범위별 표본화(4/4/3점) 후에도 화면엔 1년 2점·2년 2점만 보이던 문제. 원인 실데이터 규명: 프론트가 ①**인접 동일 국면(같은 cs,ss 칸)을 하나로 병합**(안정 국면이 분기/반기 개수보다 적게)·②**과거에 '현재 칸'을 지난 표본 라벨을 통째로 억제**하는 버그. 사용자 결정(AskUserQuestion)=**표본마다 개별 점**. 프론트 `buildRegimePath`+`stopLabelGroups` **제거** → 신규 **`buildSampledTrajectory(rawPoints, livePos)`**: 각 표본을 개별 노드로(병합 없음), 같은 칸 반복은 작은 **대각 오프셋**(`_OFFSET`·클램프)으로 분리(**판정 원값 `cs/ss/regime`·`baseX/baseY` 보존** — 오프셋은 표시 좌표만), **가장 최근 표본이 라이브 칸이면 `deferToLive`**(라이브 마커가 현재 대표·경로만 라이브로 종결), 라이브 칸의 **과거** 표본은 오프셋으로 살려 라벨 억제 버그 해소. `RegimeTrajectory.jsx`가 `{visible, pathD, deferLast}` 소비(개별 점/라벨·년도 그라데이션·브릿지·레거시 현재 유지). **결과(실데이터): 1년=과거3+현재(분기 4)·2년=과거3+현재(반기 4)·3년=과거2+현재(연 3)** — 기대 개수 정합. 좌표 SSOT(`regimeMarkerPos`)·색 토큰(방향/경보/hex 0)·면책·매매 API 0 불변. TDD(lib 6[개별 노드·오프셋·defer·과거 라이브칸·빈입력]·컴포넌트 갱신). **적대적 3렌즈 검증(정확성·계약·안전접근성) → 결함 0**. 프론트 363 green·build clean. 실데이터 프리뷰 사용자 확인 | frontend/(lib/regimeTrajectory·RegimeTrajectory·CLAUDE) + 테스트 | 사용자: 분기/반기 개수만큼 점이 안 보임 → 표본마다 개별 점 |
| 2026-07-20 | **국면 궤적 — 범위별 표본 간격(1년=분기·2년=반기·3년=연)**. 궤적이 월별이라 창이 길수록 점이 과밀·불규칙(라벨이 국면 전환마다 찍혀 "기준"이 모호)했음 → **범위에 맞춰 성기게 표본화**. 순수 `trajectory_step(months)`(범위→(step,interval코드): ≤12=분기3·≤24=반기6·else=연12) + `downsample_trajectory(points, step)`(`macro/regime_history.py` — **가장 최근 확정월 앵커**로 step 뒤로 등간격[라이브 마커/브릿지 연결 보존]·2점 이하 무표본·창<step 과박이면 과거점 보강 최소2점). **정상 12/24/36개월 = 정확히 4/4/3점**. 라우트 응답에 `interval`(quarterly/semiannual/annual, 구 `"monthly"` 폐기)+`step_months` 추가. 프론트 `INTERVAL_LABEL`로 **범례에 "분기별/반기별/연별" 표기**(기준 명시·`trajectory_step` 코드 SSOT). **판정은 안 바꾼다** — build_trajectory(엔진 재현) 결과의 표시 밀도만(월별 판정·캐시·graceful·좌표 SSOT·면책 불변). 지난 **년도별 라벨 그라데이션** 유지. TDD(백엔드 순수 5[범위매핑·앵커표본·연간3점·무표본·과박가드]+라우트 1[12개월→4분기점], 프론트 1[범례 간격 라벨]). **적대적 3렌즈 검증(정확성·계약·안전) → 결함 0**. 백엔드 877+프론트 365 green·build clean. 실데이터 프리뷰(12→4·24→4·36→3) 사용자 확인 | macro/(regime_history·CLAUDE)·api/(main·CLAUDE)·frontend/(RegimeTrajectory·CLAUDE) + 테스트 | 사용자: 궤적 표본 간격 명확화(1년분기·2년반기·3년연) |
| 2026-07-20 | **국면 궤적 — 정차점 월 라벨 년도별 밝기 그라데이션**. 궤적 라벨이 전부 균일 회색(+opacity 페이드)이라 "언제"의 시간 방향이 약했음 → **같은 해=같은 짙기, 과거년도 옅게 → 최근년도 짙게**. 순수 `labelYearShades`(`lib/regimeTrajectory.js`) 가 각 라벨 그룹의 **대표 연도**(그룹 내 최근 startDate 연도 — 재방문 셀이 여러 해면 최근 해)로 화면의 서로 다른 연도를 오래→최근 정렬해 **4단계 레벨**(0..3, `LABEL_SHADE_LEVELS` SSOT)에 매핑(단일연도/연도불명=0=현 회색 유지). 컴포넌트가 shadeLevel→`.rtraj__stoplabel--y0..y3` 클래스로 붙이고 **구 opacity 페이드 제거**(색이 최근성 전달). **색 램프 = 텍스트 위계 토큰만**(muted→secondary→text→navy) — **가격 방향색(빨/파)·경보색(빨강)·주황·하드코딩 hex 안 씀**(팔레트 규칙). `color-mix`/인라인 `var()` 대신 **CSS 클래스**로 해 jsdom 테스트 견고·토큰 SSOT 유지. 판정=백엔드 불변·좌표 SSOT(`regimeMarkerPos`) 불변·면책 고정·매매 API 0. TDD(순수 6: 연도 키잉·오래0→최근max·같은해 동일·단일연도0·재방문 최근해·graceful + 컴포넌트 1: y0/y3 클래스). **적대적 4렌즈 검증(계약·안전팔레트·접근성·엣지) → 결함 0**. 프론트 364 green·build clean·안전 grep 0. 실데이터 프리뷰(2024~2026·17정차점) 사용자 확인 | frontend/(lib/regimeTrajectory·RegimeTrajectory·styles·CLAUDE) + 테스트 | 사용자: 궤적 년도라벨 그라데이션(과거 옅게·최근 짙게) |
| 2026-07-19 | **CI/CD 액션 Node20 deprecation 해소 — GitHub Actions 버전 일괄 상향**. CI 런마다 뜨던 "target Node.js 20 but forced to run on Node.js 24" 경고 제거 → 액션 5종을 **node24 런타임** major 로 상향: `actions/checkout@v4→v7`·`actions/setup-node@v4→v7`·`google-github-actions/auth@v2→v3`·`google-github-actions/setup-gcloud@v2→v3`. **setup-uv 는 예외** — v8 부터 floating major/minor 태그(@v8)를 발행하지 않아(refs/tags/v8 404) `@v5→@v8.3.2` **전체 버전 핀**(인라인 주석). **검증(적대적, 환각 방지)**: 각 major 태그가 실제 존재하는지 `gh api git/ref/tags/<tag>` + action.yml `runs.using:node24` 를 결정적으로 확인, breaking change 를 우리 사용 맥락과 대조(checkout v7 fork-PR 차단은 우리 트리거 push/PR 무관 · setup-uv v8 python-version 입력 유지 · setup-node cache:npm 유지 · **auth v3 가 WIF 핵심 입력 workload_identity_provider/service_account 유지**[제거된 건 deprecated 파라미터] · setup-gcloud 옵션 없이 사용). YAML 유효성·구버전 잔존 0 확인. 앱 빌드 node-version:"20" 은 액션 런타임과 별개라 유지(별도 결정). WIF 미설정 시 deploy skip 계약 불변 | .github/workflows/ci-cd.yml·CLAUDE | 사용자: Node20 deprecation 경고 지금 정리 |
| 2026-07-19 | **시장 국면 판정 매트릭스 ↔ 궤적 사분면 통합(단일 사분면)**. 판정 2×2(RegimeGauge 정적 `.quadrant`)와 궤적 SVG 사분면이 **세로로 겹쳐 중복**(사용자 지적, 사분면이 더 직관적) → **궤적 SVG 하나로 통합**(정적 `.quadrant` 제거). 설계 패널 3안(minimal/hero/scannable) 수렴 + 사용자 선택 **적당히**. `RegimeGauge` 가 라이브 판정(`fetchMacroRegime`)을 **`<RegimeTrajectory live={{cs,ss,regime,cash,confidence,cycleSign,sentimentSign}} />` 옵셔널 prop** 로 주입(없으면 하위호환), 궤적은 history 조회 유지 — **좌표는 `regimeMarkerPos` 단일 함수(SSOT)** 로 라이브 마커·경로 공통 통과(포크 0). 통합 사분면: **활성 셀 음영**(현재 국면 1칸 `--c-emph-soft`)·**라이브 마커**(주황+흰 링+SVG 펄스 `rtraj-pulse` transform scale, marker-ring[HTML box-shadow] 대체)·**콜아웃**·**경기/심리 readout**·경로 마지막 정차점 회색 강등(현재=라이브 마커)·**브릿지**(라이브≠확정월 시 주황 파선). **라벨 겹침 수정(사용자 후속)**: 재방문 좌표 정차점의 시작월을 **`stopLabelGroups` 로 `", "` 병합**(예 `24.01, 24.03`). 현금비중·신뢰도·지표카드·손실경고·note 는 사분면 밖 카드 유지. 고아 CSS(`.quadrant*`/`.qcell*`/`marker-ring`) 정리. **안전**: 백엔드 0·판정=코드(라이브+엔진, LLM 0)·좌표 SSOT·색 토큰만(주황=현재/회색=과거·방향색/경보색 0·hex 0)·면책 고정. TDD(RegimeGauge 통합계약·live 유무·라벨 병합·하위호환). **적대적 4렌즈 검증 → 3건 수정**: (medium) RegimeGauge 에러분기 `onClick={load}`(미정의 식별자→ReferenceError 크래시) → `reload`(useFetch 계약) · (low) 레거시(live 없음) 경로에서 '현재' 범례 표기 복원 · (low) **라이브 셀 == 마지막 확정월 셀(안정 국면 상시)일 때 회색 월 라벨이 '현재' 콜아웃과 겹침 → 그 셀 라벨 억제**(`labelSource`). 프론트 357 green, build clean, 실데이터 프리뷰(라이브 확장+음영+경로) | frontend/(RegimeGauge·RegimeTrajectory·lib/regimeTrajectory·styles·CLAUDE) + 테스트 | 사용자: 판정 매트릭스+궤적 사분면 통합(사분면 꾸미기) |
| 2026-07-19 | **국면 이동 궤적 단순화 — 정차점 경로 + 끝점 화살표 하나**. 시황(시장 국면) 궤적이 36개 월별 점을 황금각 나선으로 흩고 전환마다 화살표를 붙여 **너무 복잡**했음(사용자 피드백) → **인접 동일 국면 월들을 하나의 정차점으로 접고**(`lib/regimeTrajectory.buildRegimePath` — dwell=머문 개월수, 셀 중심 좌표·오프셋 0) 셀 중심을 잇는 **깔끔한 폴리라인 하나** + **끝점 화살표 하나**(→현재)로 정리. 정차점은 과거 회색 흐림→현재 주황, 크기는 dwell 로 은은하게. per-transition 화살표(`.rtraj__seg`)·`buildTrajectory`/`offsetForVisit`/`trailOpacity` 폐기, 기본 기간 36→**24개월**. `regimeMarkerPos`(게이지 마커 SSOT)·판정 백엔드·면책·graceful 불변. TDD(lib buildRegimePath·컴포넌트 단일 화살표/seg 0). 프론트 348 green, build clean, 안전 grep 0. 실데이터 프리뷰(24·36개월 정차점 경로) | frontend/(lib/regimeTrajectory·RegimeTrajectory·styles·CLAUDE) + 테스트 | 사용자: 이동경로 화살표가 너무 복잡 → 단순화 |
| 2026-07-05 | 초기 구성 (에이전트 5 + 스킬 5) | 전체 | - |
| 2026-07-05 | 팀 도구 매핑 명시(Agent+SendMessage+Task*), KIS 미확정 함수 분리, 설명 질문 트리거 제외 | skills/invest-dev, skills/kis-data-pipeline | 정합성 감사 권고 반영 |
| 2026-07-05 | TDD 원칙 내장: tdd-workflow 스킬 신설, 구현 에이전트 4명 TDD 원칙 추가, QA에 TDD 준수 검증(§3.5) 추가, 오케스트레이터 Red→Green→Refactor 강제 | skills/tdd-workflow(신규), agents/*, skills/invest-qa-checklist, skills/invest-dev, skills/quant-engine-rules | 사용자 요청: 구축 흐름에 TDD 적용 |
| 2026-07-17 | **backend-architect 에이전트 신설(백엔드 총괄 리드·아키텍처 거버넌스·리팩토링·기술부채)** + `backend-architecture` 스킬(계층 판정=코드/LLM=설명·SSOT 3중일관성·경계 계약 설계·캐시 3원칙·안전 불변식·행동보존 리팩토링·QA 분업). 오케스트레이터에 팀원 추가(투입 기준=교차 관심사·리팩토링·아키텍처 점검·QA 반복 이슈 시만·상시 아님)·데이터흐름(아키텍트↔QA 루프)·에러핸들링·리팩토링 시나리오 반영. **QA와 역할 분리**: QA=동작 검증(반응형), 아키텍트=구조 거버넌스(선제형). 에이전트 5→6, 스킬 8→9 | agents/backend-architect(신규), skills/backend-architecture(신규), skills/invest-dev | 사용자 요청: 백엔드 총괄+리팩토링/아키텍처 점검 에이전트 필요 |
| 2026-07-17 | **프로젝트 전반 중복 통합·확장성 리팩토링(행동 보존, 건별 커밋)**. 3-lens 탐색으로 Rule-of-Three 초과 중복을 인벤토리 후 `refactoring` 스킬 규율(행동 보존·테스트 green·기능 분리)로 항목별 통합. **백엔드**: `chat/structured_summary.py`(4개 LLM summarizer의 make_client·request_json·parse_and_validate·재시도 코어 통합, 새 summarizer 70%↓)·`api/_helpers.graceful_counts`(5개 수집/색인 라우트 try/except+카운트 폴백)·`chat/report_progress.run_batch`(2개 서비스 비스트림 배치 루프)·`infra/parallel.fetch_parallel`(번들 섹션·워치리스트 시세·스파크 3개 ThreadPool, partial_failure 시맨틱 보존). **프론트**: 죽은코드 `StockReport.jsx`(280줄) 삭제·`lib/format.js`(포맷터 SSOT — 동작별 다른 이름[signedPct%≠signedNum·changeDir null≠flatDir flat]으로 원본 보존)·`lib/useFetch.js`(조회 상태 훅, RegimeGauge·BalancePanel 이관). **의도적 제외**(refactoring 규율): `resolve_kis_client` SSOT 이동(중복 아닌 단일 정의+monkeypatch 경계 위험), api.js 래퍼(테스트 mock-out으로 안전망 0·27함수·characterization 필요 → 실익 대비 과대). 전 항목 기존 테스트 무변경 통과(행동 보존): 백엔드 809→817(+신규 8)·프론트 301→312(+신규 11) green·build clean·안전 grep(주문 API·hex·방향색) 0 | chat/·api/·infra/·watchlist/·frontend/lib·컴포넌트 + 테스트 | 사용자: 중복 공통요소 통합·확장성 리팩토링 |
| 2026-07-17 | **`refactoring` 스킬 분리 신설**(하네스 피드백). backend-architecture에 섞여 있던 리팩토링 실행 규율(행동 보존·characterization 테스트·작은 단계·기능 분리·냄새→리팩토링 카탈로그·안전 불변식 보존·검증 체크리스트)을 **독립 스킬**로 분리 → 백엔드뿐 아니라 프론트 등 **어느 구현자든 재사용**(tdd-workflow의 Refactor 단계 심화). backend-architecture §7은 "리팩토링 **대상 진단**"만 남기고 실행은 refactoring 위임(중복 제거). backend-architect·오케스트레이터·TDD 원칙에 배선. 스킬 9→10 | skills/refactoring(신규), skills/backend-architecture, agents/backend-architect, skills/invest-dev | 사용자 하네스 피드백: 리팩토링 전용 스킬 |
| 2026-07-05 | UI 디자인 시스템 신설: 흰색/회색/파랑/남색/검정 팔레트 + theme.css 토큰 SSOT, frontend-engineer 원칙0에 반영, 대시보드 라이트 톤 적용 | skills/ui-design-system(신규), agents/frontend-engineer, frontend/src/theme.css·styles.css | 사용자 요청: UI 톤 통일 |
| 2026-07-05 | doc-commit 스킬 신설, W06 완료 + 디렉토리별 CLAUDE.md 정리(collectors/cache/api/frontend), 첫 커밋 | skills/doc-commit(신규), 각 디렉토리 CLAUDE.md, CLAUDE.md | 사용자 요청: 진행상황 문서화 후 커밋/푸시 |
| 2026-07-06 | W07 매크로 엔진(2축 경기×심리 + 역발상 현금비중) + /api/macro/regime + 2×2 게이지 UI | macro/(engine·CLAUDE), api/, frontend/(RegimeGauge) | 사용자 결정: 2축 판정 + 역발상 |
| 2026-07-06 | UI 강조/위험 색 체계: 주황(강조)·빨강(위험) 토큰 추가, 2×2 위치 점·국면 해설 | skills/ui-design-system, frontend/theme·styles | 사용자 요청: 강조=주황, 위험=빨강 |
| 2026-07-07 | W08 종목 종합리포트: 정량요약 엔진(CAGR·avg_per 자기과거평균·RSI/MA/52주·regime_gate 역발상) + KIS 어댑터 4종(현재가·손익·재무비율·추정실적)+번들 API(partial_failure·캐시게이트) + 예측 PER(리서치 컨센서스, 후행 PER 보완) + KLineChartPanel(klinecharts) + 종목명 자동완성(KIS 마스터). avg_per는 라이브 검증 게이트. 218 tests green | stock/·collectors/·api/·frontend/ + 각 CLAUDE.md | WEEK 08 로드맵 + 사용자 요청(예측 PER·자동완성) |
| 2026-07-07 | 캔들차트·등락률 한국 관습색(상승=빨강/하락=파랑) — 팔레트 유일 예외로 문서화(theme.css `--c-chart-*`); LLM agent 패턴 추출(강의 노트북→llm-safety-guide references, 노트북 삭제) | skills/ui-design-system·llm-safety-guide(신규 references), frontend/theme·styles·theme.js, collectors·frontend CLAUDE.md | 사용자 요청: 한국식 차트색 + 노트북 LLM로직 지침 반영 후 삭제 |
| 2026-07-09 | W09 LLM 챗봇: build_prompt(기준표 자동생성)·chat(agent 루프)·팝업 3종·서버 세션 + **ML 인텐트 6분류**(gpt-5.4 데이터 생성→sklearn 학습, 결정적 키워드 가드레일 우선) + 프론트 챗 UI(ChatPanel·팝업 모달·ticker SSOT). 모델 gpt-5.4(CHAT_MODEL 단일). P2(Pydantic 리포트 스키마)는 W10 이월 | chat/(신규·CLAUDE)·api/·macro/(INDICATOR_LABELS)·frontend/ | WEEK 09 로드맵 + 사용자 결정(gpt-5.4·ML 인텐트·서버 세션) |
| 2026-07-09 | 챗봇 응답 UX = **SSE 실시간 스트리밍**(진행 단계 체크리스트 + 토큰 타이핑). chat_stream 제너레이터(스트리밍 tool_calls 재조립)+POST /api/chat/stream, 프론트 sseChat(순수 파서)·chatStages·ChatPanel 스트리밍 상태기계. 기존 chat()·/api/chat 병행(폴백). guardrail은 스트리밍에서도 LLM 미호출·live_judgement 미실행 | chat/·api/·frontend/ + 각 CLAUDE.md | 사용자 요청: "답변준비중" 정적표시 개선 |
| 2026-07-09 | 로컬 도커 기동 구성(백엔드 py3.13+uv, 프론트 node20+Vite 2컨테이너, .env 런타임 주입·핫리로드·볼륨 격리). Vite 프록시 대상 env화 | Dockerfile·docker-compose·DOCKER.md·frontend/vite.config | 사용자 요청: 로컬 기동 도커화 |
| 2026-07-09 | **W10 워치리스트(모듈 3) — 5주 로드맵 완성**: CRUD 라우트(GET/POST[409 상한]/DELETE/PATCH)·durable JSON store(`(user_id,ticker)` DynamoDB 계약, 단일 로컬 사용자)·진입신호(`regime_gate` 재사용, `single_cap` 게이트 **regime-agnostic**)·종목별 `inquire_price` 병렬 + 팝업 겸 독립 페이지 `WatchlistView`·능동 목표가 알림(App 레벨 전이 배너/Notification·60s) + **[P2] 구조화 리포트**(`StockReport` Pydantic 안전강제[리스크 min1·면책 필수·종합의견 Literal]·생성 폴백·`.cache` 히스토리). 백엔드 403 + 프론트 94 green, QA GO | watchlist/(신규·CLAUDE)·api/(watchlist·report)·chat/(report*·build_prompt)·frontend/(WatchlistView·AiReportPanel·watchlistLogic·reportFormat) + 각 CLAUDE.md | WEEK 10 로드맵 + 사용자 결정(파일 단일사용자·능동 알림·P2 리포트 포함) |
| 2026-07-10 | **코드리뷰 백로그 20건(IMP-01~20)** 반영 — 종합 코드리뷰로 P0~P2 우선순위 백로그 도출 후 항목별 TDD(Red→Green)·개별 커밋. `api/deps.py` 공용 SSOT(ticker 검증·국면 매핑), 워치리스트 편집 확인 팝업(`manage_watchlist`, 자동 매매 아님), 목표가 상태 매수관점 통일, jsdom 컴포넌트 테스트 인프라(IMP-17), Store 동시성 회귀 등. 백엔드 403→455·프론트 89→104 green | api/·chat/·watchlist/·frontend/ 전반 + 테스트 | 사용자 요청: 마감 후 개선점 도출·P0부터 순차 |
| 2026-07-10 | **UX 개편 — 좌측 상시 채팅 + 우측 동적 패널(팝업 모달 폐기)**: `App` 2컬럼 그리드, `RightPanel`(챗 tool_call·퀵버튼 병행 구동·랜딩=관심종목), `Modal.jsx` 삭제, 목표가 알림 폴링 App 레벨 이관. + **잔고(포트폴리오) 패널 신규**: `GET /api/balance`(조회 전용·무캐시·graceful, 기존 KIS 어댑터 재사용)·`infra.config.kis_account()`·`show_balance` 툴(데이터는 프론트 조회, 리밸런싱 조언은 텍스트만)·`BalancePanel`(손익 파랑/회색 팔레트). 백엔드 472 + 프론트 128 green, QA GO(안전위반 0) | api/(balance)·infra/·chat/(tools·build_prompt)·frontend/(App·RightPanel·BalancePanel·ChatPanel·popupRouter) + 각 CLAUDE.md | 사용자 요청: 하단 채팅·팝업 부자연 → 좌채팅+우패널, 잔고 포함 |
| 2026-07-11 | **UX 리디자인 "Refined Pro" Phase 0(Foundation)** — 팔레트 방향색 반전(사용자 확정): 상승·수익=빨강(`--c-up`#e5322d)/하락·손실=파랑(`--c-down`#2563eb)/보합=회색(`--c-flat`)을 **전 화면** 적용(차트만의 예외 → 전역). 토큰만 교체해 컴포넌트 무수정 반영. + Pretendard 폰트·tabular-nums, 신규 토큰(브랜드/네이비딥/소프트방향/셸 라운드), 키프레임 5종(marker-ring·rise-in·shimmer·flash-up/down)+`.skeleton`. 빨강 경보(`--c-danger`)는 채움형 배너/칩+⚠로 상승 빨강과 형태 구분. 색 규칙 문서 3곳(ui-design-system·frontend/CLAUDE·메인) 개정. 프론트 169 green, build clean | index.html·theme.css·styles.css + 문서 + `_workspace/design_handoff_dk_invest_redesign/` 스펙 | 사용자 UX 리디자인(Phase 0, A~E 후속) |
| 2026-07-11 | **CHAT_MODEL 변경 `gpt-5.4` → `gpt-5.6-luna`**(사용자 결정) — `chat/tools.py::CHAT_MODEL` 단일 출처 값만 교체(코드는 전부 상수 참조라 무산재). 이를 고정하던 `test_tools.py` 단정·문서(chat/CLAUDE·메인) 갱신, 부수 주석은 리터럴 대신 `CHAT_MODEL` 참조로 일반화. 483 green | chat/tools.py + test + 문서 | 사용자 요청: 모델 교체 |
| 2026-07-11 | **리디자인 Phase A~E 완료** — A: 톱바 리브랜딩(DK 모노그램 SVG·파랑 워드마크·국면/현금비중/VIX 상태 칩[App이 `fetchMacroRegime` 자체조회]·알림 CTA 상태·주황 알림 배너), 챗 빈상태 제안칩 4·유저 네이비 버블·rise-in. B: 우측 패널 퀵버튼→**세그먼트 탭**(활성 네이비)+인라인 종목검색+**전환 스켈레톤**(450ms, 초기 마운트 제외). C: **WatchlistView 테이블→카드 로우**(등락 칩 소프트 배경·목표가 근접 게이지[매수 관점]·진입 배지·TargetCell 인라인 편집 유지)+**스파크라인**(`spark` SVG, 선색=방향색), RegimeGauge 마커 펄스+**네이비 현금비중 카드**(주황 값)+2컬럼, **BalancePanel 네이비 히어로 카드**(순자산+손익 pill)+보조카드4+dashed 부분실패, 리포트 대형 가격+등락 칩·국면정합성 주황 소프트 카드, ManageWatchlistConfirm 확인=주황(기존 준수). 백엔드 483 + 프론트 174 green, build clean, 안전 grep 0(주문 API·현재가 캐시[spark 무캐시]·hex 하드코딩·`--c-danger` 오용 없음, POPUP_KIND↔RightPanel 5종 일치, 캔들 예외 토큰·`lib/theme.js` 불변) | frontend/(App·RightPanel·ChatPanel·WatchlistView·BalancePanel·styles) + 테스트 갱신 | 리디자인 A~E 직접 완성(에이전트 팀 이름충돌로 재시작 후 solo) |
| 2026-07-11 | **KIS 500 근본원인 해결 — 좀비 토큰 자가치유**: 시세·잔고·재무·추정이 "조회 불가"(HTTP 500)였던 원인 규명. ①`client.py`가 `raise_for_status()`로 **5xx 본문을 버려** 진짜 에러를 못 봄 → **본문 표면화**(msg_cd/msg1/status를 `KisApiError`로)+WARNING 로깅. 이게 실제 원인 `EGW00123 기간이 만료된 token`을 드러냄(rate-limit 아님·스파크 무고 — 초기 가설 정정). ②캐시 토큰이 **외부 재발급으로 KIS에서 무효화**됐는데(우리 `expires_at`은 미래) 코드가 죽은 토큰을 좀비 재사용 → `EGW00121/00123` 감지 시 **`provider(stale_token)` 강제 재발급 후 재시도**(auth.get_token 동시 재발급 방지). ③전이성 5xx·유량(EGW00201) backoff 재시도. 라이브 실증(토큰 오염→자가치유 복구) + 전 엔드포인트 실값 복구. 488 green | collectors/kis/(client·auth·errors) + test + collectors/CLAUDE | 사용자: 리디자인 후 KIS 안 보임 → 원인해결 |
| 2026-07-11 | **모델 gpt-5.6-luna 호환** — 이 모델(추론형)은 chat/completions+function tools 에 `reasoning_effort='none'` 필수(미지정 시 400). `CHAT_MODEL_PARAMS`(chat/tools.py 단일출처)를 매 create()에 병합(`_create_with_retry`[스트리밍]·report·intent_gen). 챗 "답변 생성 불가" 해결 | chat/(tools·chat·report·intent_gen) | 사용자: 사이트 답변 안 됨 |
| 2026-07-11 | **W09 강의요소 통합 — YouTube 자막 요약 + 증권사 리포트 PDF RAG**. ①**콘텐츠 툴** 도입(표시 툴과 구분): `tools.py::CONTENT_TOOLS`+`run_content_tool` 단일출처, `chat.py`(chat·chat_stream)가 서버 실행→실제 텍스트 되먹임(팝업 제외). ②`summarize_youtube`(`collectors/youtube.py`, youtube-transcript-api, **타임아웃 12s** hang 방지·graceful). ③**리포트 RAG** `rag/`(ingest:pdfplumber+표보존청킹 · embed:text-embedding-3-small · store:**numpy 코사인**[FAISS 대신]·.cache 영속) + `POST /api/reports/reindex`·`search_report` 툴. ④안전: build_prompt ⑧블록(외부 콘텐츠="영상/리포트에 따르면" 출처귀속·매매판정 금지·면책). ⑤보안: 강의 파일 하드코딩 키 제거. 라이브 e2e(YouTube 요약·리포트 목표주가 검색 둘 다 출처귀속+면책). 523 green | collectors/youtube·rag/·api/reports·chat/(tools·chat·build_prompt) + 테스트 + `reports/` gitignore | 사용자: W09 강의파일 요소 통합(둘 다) |
| 2026-07-13 | **대화창 이름 지정/수정(항목2) — 자동 명명 + 수동 편집**. 대화 제목이 전부 "새 대화"라 구분 불가 → ① **첫 질문 자동 명명**(`history_store.add_turn` 이 제목이 아직 default 면 첫 user_text 로 `_derive_title`[공백정리·30자 말줄임], 요약 LLM 아님·비용0) + ② **✎ 수동 편집**(`rename_conversation` 소유권 게이트 + `PATCH /api/conversations/{id}`[빈제목 422·남의대화 404]). `DEFAULT_CONVERSATION_TITLE` SSOT 상수. 프론트 `api.renameConversation`·`App.handleRenameConversation`+턴완료 `refreshConversations`(자동명명·재정렬 반영)·`ChatPanel` 스위처 옆 ✎ 인라인 편집(input+주황 ✓/✕·Enter/Esc·빈제목 저장비활성). 유저 스코프 불변. 723+218 green, 라이브 e2e(새 대화→질문→'삼성전자 요즘 어때?' 자동명명→rename→422) + 브라우저(✎ 편집 UI) 확인 | chat/(history_models·history_store)·api/conversations·frontend/(api·App·ChatPanel·styles) + 테스트 | 사용자: 대화 이름 지정/수정 |
| 2026-07-13 | **리포트 가져오는 중 진행상황 표시(항목1, SSE 스트리밍)**. 리포트 "가져오기"(종목 애널리스트·시황)가 목록 조회→각 PDF 다운로드→OpenAI 요약 배치라 오래 걸리는데 정적 "가져오는 중…"만 떠서 뭘 하는지 몰랐음 → **SSE 실시간 진행 체크리스트**(목록 조회 중 → N건 발견[리스트] → 각 리포트 완료 틱[i/N·증권사·제목] → 완료). `chat/report_progress.iter_process_metas`(공용 제너레이터·`as_completed`) + `analyst_service.iter_fetch_and_summarize_for_ticker`·`market_outlook_service.iter_fetch_and_summarize` + `api/_sse.sse_response`(chat SSE 헤더 SSOT·에러 프레임 graceful) + SSE 라우트 2개(`…/analyst-reports/fetch/stream`·`…/market-outlook/fetch/stream`, non-stream 폴백 보존). 프론트 `lib/sse.readSSE`(parseSSEBuffer 재사용)·`FetchProgress`(체크리스트+`applyProgressEvent` 리듀서)·두 섹션 배선(끊김 시 non-stream 폴백). 이벤트 계약 SSOT(stage/found/progress/done/error). 조회·요약 전용·면책 불변. 714+215 green, 라이브 e2e(006360 실 스트림 목록·진행·완료 확인) | chat/(report_progress·analyst_service·market_outlook_service)·api/(_sse·reports·macro_outlook)·frontend/(lib/sse·api·FetchProgress·AnalystReportsSection·MarketOutlookSection·styles) + 테스트 | 사용자: 리포트 로딩 중 작업 표시 |
| 2026-07-13 | **유저별 KIS 자격증명 — 공유키 제거 + DB 암호화 저장(사용 시 복호화)**. 치명적 결함(KIS 앱키가 전 유저 공용) 해소: 유저가 본인 KIS 키를 등록하면 그 키, 미등록/비로그인은 **공유 fallback**(잔고 포함) 사용(사용자 결정). Phase 1 `infra/encryption.py`(Fernet·마스터키 `KIS_ENC_KEY`). 2 `auth/kis_(models·store)`(scope_key=`str(user.id)`|`__shared__`, 암호문만, `resolve`/`status` 마스킹). 3 `resolve_kis_client(user,db)`(본인→공유→env) + **토큰 캐시 app_key 별 재키잉**(`kis_token_key(env,app_key)` sha256, 자가치유 불변) + `get_current_user_optional`. 4 `POST/GET/DELETE /api/me/kis-credentials`(등록 시 **실제 KIS 토큰 발급으로 검증 후** 저장·실패 400·키값 미노출·GET 마스킹만). 5 balance/detail/report/watchlist/view_context 를 옵션 인증+`_resolve_client` 로 배선(공개 유지·graceful). 6 프론트 `KisSettingsPanel`(설정 탭)·authFetch 전환. 7 `seed_shared_kis_from_env()`(startup, env→`__shared__` 암호화 1회 시드) + 프로덕션 전환(KIS_ENC_KEY 등록 → 전환배포 시드 → KIS 앱키 시크릿 제거·재배포). **KIS 앱키는 이제 Secret Manager 에 없음**(암호화 DB만). 안전: 평문 저장·로깅·응답 0, 복호화는 사용 직전 in-memory. 백엔드 706 + 프론트 208 green, 로컬 e2e(암호화·검증400·격리·공유 fallback) | infra/(encryption·config·db)·auth/(kis_models·kis_store·kis_seed·deps)·collectors/kis/auth·cache/keys·api/(kis_credentials·detail·balance·report·watchlist·chat·main)·chat/view_context·frontend/(api·KisSettingsPanel·RightPanel) + 테스트·문서 | 사용자: KIS 앱키 유저별 분리·Secret Manager 제거·DB 암호화 |
| 2026-07-12 | **GCP 프로덕션 배포(Cloud Run + Cloud SQL) — 5주 로드맵 잔여 P2 완료**. 승인 플랜대로 **단일 Cloud Run 서비스**: FastAPI 가 `/api/*` + 빌드된 React `frontend/dist` 를 **같은 오리진**에서 서빙(프론트 전부 상대경로 `/api` → CORS·프론트 코드 변경 0, SSE 그대로). 아티팩트: 루트 `Dockerfile`(프로덕션 멀티스테이지 — node 빌드→python 런타임·`uvicorn $PORT` no-reload), 기존 dev 는 `Dockerfile.dev`(compose 가 참조), `api/main.py` 말미 dist 정적서빙+**404 예외 핸들러 SPA 폴백**(비-API GET 만 — catch-all GET 은 `/api` 405 를 가려 금지, `/api` 405/JSON 에러 불변, dist 없으면 skip→로컬·테스트 무영향), `psycopg[binary]` 추가, `.dockerignore`(frontend 제외 해제·granular)·`.gcloudignore`·`DEPLOY_GCP.md`. **인프라**(GCP): 새 프로젝트 `dk-invest-agent-2607122107`(서울 `asia-northeast3`)·과금 연결·Cloud SQL `dk-invest-db`(POSTGRES_15·db-f1-micro, DB `appdb`, unix socket)·**Secret Manager 8종**(OPENAI/KIS×4/FRED = 사용자 `.env`에서 본인 실행 + JWT_SECRET·DATABASE_URL = 랜덤 생성)·런타임 SA secretAccessor. **안전**: API 키 시크릿 등록·IAM 부여는 사용자가 직접 실행(자격증명 입력·접근제어 변경 자동화 금지), `.env`/시크릿값 이미지·업로드·로그 미포함. 라이브 검증(health·SPA·regime[FRED]·회원가입/로그인[Cloud SQL R/W]·잔고[KIS]·관심종목[KIS+DB+진입신호]·챗[OpenAI] 전부 200/정상)·브라우저 렌더 확인. 677+203 green | Dockerfile(prod)·Dockerfile.dev·docker-compose·.dockerignore·.gcloudignore·api/main.py·pyproject·infra/db + DEPLOY_GCP.md·CLAUDE | 사용자: GCP 클라우드 배포 |
| 2026-07-12 | **종목별 애널리스트 리포트 수집 수정** — 종목 상세의 "가져오기"가 `/api/reports/fetch`(전체 최신 피드, 전 종목 섞임)를 불러 **대부분 종목 섹션이 비어 보이던 문제**. 네이버 `company_list.naver?searchType=itemCode&itemCode=<ticker>` **종목 필터**(라이브 확인)로 `collectors.naver_research.fetch_stock_reports`(+공용 `_fetch_list` 리팩터) → `analyst_service.fetch_and_summarize_for_ticker` → `POST /api/detail/{ticker}/analyst-reports/fetch`(1~30 클램프·graceful) → 프론트 `fetchNaverStockReports(ticker)`·버튼 "이 종목 리포트 가져오기"로 배선. 라이브 e2e(005930·006360 각 종목만 수집·저장 확인). 겸사 `test_analyst_service`의 인메모리 StaticPool 병렬 upsert flaky를 **파일 SQLite(워커별 커넥션)+busy_timeout**으로 해소. 백엔드 677 + 프론트 203 green | collectors/naver_research·chat/analyst_service·api/reports·frontend/(api.js·AnalystReportsSection) + 테스트 + 문서 | 사용자: 종목별 애널리스트 보고서 안 가져와짐 |
| 2026-07-11 | **네이버 애널리스트 리포트 수집·요약 + 챗 상담 컨텍스트 연계**. 수집(`collectors/naver_research.py` — company_list SSR HTML·EUC-KR·robots 허용·PDF 직다운·예의 크롤링) → 보고서별 구조화 요약(`chat/analyst_schema.py`·`analyst_report.py`, report.py 패턴: `AnalystReportSummary` 안전강제[핵심요지·리스크 min1·면책 필수]·생성·검증·재시도·폴백) → ticker 키 idempotent store(`analyst_store.py`, report_id 중복 skip) → 병렬 오케스트레이션(`analyst_service.py`). API `POST /api/reports/fetch`·`GET /api/detail/{ticker}/analyst-reports`·`POST /api/chat/report-context`. **세션 핀 컨텍스트**(`Session.report_context`, 슬라이딩 윈도우와 별개) → `_build_system_prompt` 주입(출처 귀속·판정 금지·면책, **가드레일 불변**). 프론트 `AnalystReportsSection`(자체조회) + "이 리포트로 상담하기"(주황 CTA→좌측 챗 배너)·**세션 id App 단일 소유**. 안전: 요약=리포트 인용(에이전트 판정 아님)·요약본문 프론트 신뢰전송 없음. 라이브 e2e(GS건설 다운로드→pdfplumber→요약→검증)·569 + 179 green | collectors/·chat/(analyst*·session·chat)·api/(reports·chat)·frontend/(AnalystReportsSection·App·RightPanel·ChatPanel·StockReportView·api.js) + 각 CLAUDE | 사용자: 네이버 애널리스트 PDF 보고서별 요약 + 요약을 세션에 실어 후속 자문 |
| 2026-07-12 | **챗 "현재 보는 화면" 컨텍스트 연계** — 표시 툴이 `{ok:True}`만 되먹여 챗봇이 잔고·관심종목·종목상세(애널리스트 요약 포함) 숫자를 몰라 후속 대화 불가였던 문제 해결. `Session.view_context` 핀 슬롯(report_context와 별개)·`_build_system_prompt` 주입(_VIEW_CONTEXT_HEADER: 인용만·숫자 날조 금지·기준시각·판정 금지·면책)·`chat/view_context.py::build_view_context`(서버 재조회 컴팩트 스냅샷: 잔고 `inquire_balance`·관심 `build_watchlist_view`·종목 경량 `inquire_price`+`regime_gate`+애널리스트, top-N·1500자·절대 예외 없음)·`POST /api/chat/context`(kind/args만, 서버 조회—프론트 신뢰전송 없음). **P1** 프론트 `App.jsx` 패널변경 useEffect 핀(디바운스·중복스킵·해제) + **P2** `chat.py::_display_tool_result`가 뷰 툴 결과에 스냅샷 실어 같은 턴 즉답(`tools.py::VIEW_CONTEXT_TOOLS`, 팝업 유지). build_prompt ⑦완화(스냅샷 숫자 인용 허용). guardrail 선차단 불변. 챗 유닛테스트는 build_view_context 기본 stub(requests_cache 오염 방지). 라이브 e2e(잔고 3종·P2 챗 즉답 골프존 -22.90% 인용+면책)·600 + 182 green | chat/(session·chat·view_context·tools·build_prompt)·api/chat·frontend/(App·api.js) + 각 테스트·CLAUDE | 사용자: 조회결과 맥락을 챗이 파악 못해 대화 불가 → 컨텍스트 연계 |
| 2026-07-12 | **챗 자문 완화 — 인텐트 분류기 개선 + LLM 재분류 원칙 + 코드근거 상담**. 문제: ML 분류기가 "내 잔고 확인/손실 난 종목 알려줘"를 risk_guardrail 로 오분류→하드블록(실측), 프롬프트 과도 제한으로 포트폴리오 자문 불가. ①**분류기 개선**: 정당 조회 표현을 정답 라벨(portfolio_advice 등)로 데이터셋 +20/시드 +8 보강 후 재학습 + `test_intent.py` 데이터셋 품질 TDD(정당 조회 비차단, 결정적). ②**LLM 재분류 원칙**(사용자 결정): `guardrail_label`(결정적 4유형)은 하드블록 유지, 그 외 ML=risk 는 `chat.py::_reclassify_risk`(CHAT_MODEL `{block}`)로 2차 판정 — 오탐 구제/위험 확정 차단, 실패는 보수적 차단. `chat()`·`chat_stream()` 공통, api 라우트 judgement 스킵은 결정적일 때만. ③**프롬프트 완화**: build_prompt 블록 ⑧ [포트폴리오 상담](잔고 근거 조정 방향 + entry_allowed 추가편입 후보 + 새 종목 아이디어) + ⑤⑦ actionable 완화 + ⑥ 내부정보/시세조종 거부·수익보장 금지 강화. 불변: 단정 금지·면책·수치 날조 금지·과열 규율·매매 API 0. 라이브 e2e(잔고 확인·조정안·추가편입 정상 / 빚내서 몰빵 차단) + 브라우저 스모크(이전 차단 "손실 난 종목" → 정상 응답). 615 + 182 green | data/intent_dataset·chat/(intent·chat·build_prompt)·api/chat + 각 테스트·CLAUDE | 사용자: 손익 조회 오탐 차단 + 포트폴리오 자문 가능하게 |
| 2026-07-12 | **챗 자문 완화 — 인텐트 분류기 개선 + LLM 재분류 원칙 + 코드근거 상담**. 오탐(잔고·손익 조회가 risk 하드블록) 데이터셋 보강+재학습. **위험 2층 차단**: `guardrail_label`(결정적 4유형) 하드블록 유지 + ML=risk 는 `chat.py::_reclassify_risk`(CHAT_MODEL) 2차 판정(오탐 구제/위험 확정, 실패 보수차단). build_prompt 블록 ⑧ [포트폴리오 상담](잔고 근거 조정·entry_allowed 추가편입 후보·새 아이디어) + ⑥ 내부정보/시세조종 거부 강화. 라이브 e2e·브라우저 스모크·615 green | chat/(intent·chat·build_prompt)·data/intent_dataset + 테스트 | 사용자: 손익 조회 오탐 + 포트폴리오 자문 가능 |
| 2026-07-12 | **유저베이스 + 시황 + 대화기록 (GCP 지향 5-Phase, 각 라이브 검증·개별 커밋)**. ①시황 PDF 요약(naver_research category화·MarketOutlookSummary·시장 국면 페이지 MarketOutlookSection). ②인증+DB(`infra/db` SQLAlchemy·`DATABASE_URL` 로컬SQLite↔GCP Cloud SQL Postgres·`auth/`[User·bcrypt·JWT·get_current_user]·LoginScreen 게이트). ③유저별 관심종목(`SqlWatchlistStore` Protocol 스왑·인증 스코프·유저 격리·authFetch). ④대화기록(Conversation/ChatMessage·`HistoryStore`·`api/conversations`·챗 인증필수+hydrate+write-through·대화 스위처/새 대화). ⑤애널리스트·시황 공동 DB(`AnalystReportRow` 전역공유 scope_key·`ScopedReportRepo` 세션팩토리 스레드안전·analyst/market store SQL 어댑터 인터페이스 불변). 잔고=공유 데모계좌 유지. 비번 평문 0·JWT/DB env(gitignore). **도커: `uv add` 후 `docker compose exec backend uv sync` 필수**(backend-venv 볼륨). 668 + 203 green | infra/db·auth/·api/(auth·conversations·watchlist·chat·macro_outlook)·chat/(history*·report*·market_outlook*·analyst_store)·watchlist/(sql_store·db_models)·collectors/naver_research·frontend/(App·auth·LoginScreen·ChatPanel·MarketOutlookSection·api) + 각 CLAUDE·.env.example·DOCKER | 사용자: 시황 페이지 + 회원제 유저별 관심종목·대화기록 |
| 2026-07-13 | **시황 리포트 UX(항목4) — 일별 구분 · 3줄요약 · 클릭 상세 오버레이**. 증권사 매크로보고서(시황) 섹션이 요약을 평면 카드로 전부 인라인 펼쳐 훑기 어려웠음 → ① `MarketOutlookSummary`에 `세줄요약`(min1·max3) 추가 + `market_outlook.py` 프롬프트 3줄 압축 지시(리포트 인용·면책 불변, 구 저장 레코드는 재검증 안 하고 프론트가 `핵심요지[:3]` 폴백). ② 프론트 순수 `lib/marketOutlook.js`(`groupReportsByDate`[작성일별·날짜결측 맨끝]·`threeLineSummary`[세줄요약 우선·핵심요지 폴백]). ③ `MarketOutlookSection`을 **날짜 헤더 + 컴팩트 카드**(증권사·시장전망 칩[중립 톤]·제목·3줄요약, `<button>` 키보드 접근)로 재구성 → 클릭 시 **`MarketOutlookDetailOverlay`**(딤 배경 `--c-overlay`·중앙 카드·✕/Esc/배경클릭 닫힘·`role=dialog` aria-modal·닫기버튼 포커스·트리거 복원·배경 스크롤 잠금)로 전체 상세(요약·핵심요지·리스크·면책·PDF). **모달 폐기 관습의 의도적 예외**(사용자 결정, 시황 상세 전용·React DOM 오버레이라 이벤트 블로킹 없음, 범용 Modal.jsx 부활 아님). SSE fetch·면책 상시 불변. 안전 자체검증(hex 하드코딩 0·가격방향색/경보색 오용 0·매매 API 0·브라우저 dialog 0). 백엔드 714 + 프론트 224 green, build clean | chat/(market_outlook·market_outlook_schema)·frontend/(lib/marketOutlook·MarketOutlookSection·styles) + 테스트·각 CLAUDE | 사용자: 증권사 매크로보고서 일별 구분·3줄요약·클릭 팝업 |
| 2026-07-13 | **국면 로직 단순화(항목3) — 국면별 종목 진입게이트 폐기, 국면=현금비중만**. 국면별로 현금비중+`single_cap`+`per_max`+`pbr_max` 4가지를 관리하던 것이 "너무 보수적"(진입 차단·밸류에이션 부담 판정 과함) → 사용자 결정: **국면은 현금비중만 관리하고 PER/PBR/편입비중 커트는 다루지 않는다**. ①`REGIME_PARAMS`→`{cash}`만(역발상 현금비중 SSOT 불변). ②`stock/summary.py` `regime_gate`·`regime_entry_blocked`·`_gate_note` 삭제. ③워치리스트 `entry_signal` 필드·regime 블록 `single_cap`/`entry_blocked` 제거(regime 블록=`{regime}`만), ④번들 `regime_gate` 키 제거+**GET bundle 라우트가 미사용 `_build_judgement()`(FRED) 호출 제거**(judgement 시그니처는 report.py 호환 유지·번들 내부 미사용). ⑤`build_prompt` ④블록 재작성(현금비중만·PER/PBR 참고 데이터·국면 커트 없음)·`ENTRY_SIGNAL_RULES`/진입지침 삭제. ⑥`report.py::_regime_block` = **국면명+권장 현금비중 제시→LLM 이 '최종 적합성' 서술**(사용자 정제 요구). ⑦프론트 `entrySignalLabel`·진입 배지·국면정합성 카드(`regime_gate`) 제거, 국면 배너=국면명만, dangling CSS(`.regime-fit*`·`.wl__regime-flag/note`) 정리. 종목 raw PER/PBR·`valuation_label`(자기과거 avg_per)·CAGR·RSI·52주·스파크라인·RegimeGauge 현금비중은 유지. **적대적 다각검증(5렌즈 워크플로우)**: 안전 불변식·고아참조 clean, 계약 정합(번들↔프론트 일치) clean, 회귀 green. 백엔드 710 + 프론트 214 green, build clean | macro/engine·stock/summary·watchlist/service·api/(detail·deps)·chat/(build_prompt·report·view_context)·frontend/(watchlistLogic·WatchlistView·StockReportView·api·sampleBundle·styles) + 테스트·각 CLAUDE | 사용자: 국면별 PBR/PER/편입비중 커트 제거, 현금비중만 관리 |
| 2026-07-13 | **애널리스트 리포트 최근 3개 종합 10줄요약(항목5)**. 종목 상세의 애널리스트 섹션이 증권사별 **개별 요약 카드**만 나열해 여러 리포트를 한눈에 꿰는 종합 뷰가 없었음 → **최근 3개 리포트를 종합해 10줄로 요약**. 핵심: **저장된 per-report 요약**(`store.list_reports(ticker)[:3]`, date 내림차순)만으로 LLM 이 1회 종합 — **PDF 재다운로드·수집 0**(`format_report_context`[analyst_report.py] 재사용). ① `CombinedAnalystSummary`(`analyst_combined_schema.py`) 안전강제: `종합요약`(list min1·max10)·`면책고지` 필수·`의견분포`(리포트 투자의견 집계 "매수 2·중립 1")·`목표주가범위` optional. **`리포트수`는 스키마에 없고 코드가 계산**(`report_count`, LLM 카운트 환각 방지). ② `analyst_combined.summarize_recent_reports`(`market_outlook.py` 패턴: 종합 프롬프트[복수 출처 귀속·의견 분포·단정 금지·면책]·검증·1회 재시도·폴백·0개 LLM 미호출). ③ `POST /api/detail/{ticker}/analyst-reports/summary`(온디맨드·`assert_valid_ticker` 400·항상 200 graceful). ④ 프론트 `AnalystReportsSection` 개별 카드 **위** `CombinedSummary` 패널("종합요약 생성" 주황 CTA→`fetchAnalystReportsSummary`→의견분포 칩[중립]·목표주가범위·10줄 `<ol>`·면책). **안전**: 여러 증권사 리포트 내용의 **종합·인용**(에이전트 판정 아님)·출처 복수 귀속·면책. 자체검증(매매 API 0·hex 0·가격방향색 오용 0·면책/출처귀속 보존). 백엔드 730 + 프론트 227 green, build clean | chat/(analyst_combined·analyst_combined_schema)·api/reports·frontend/(api·AnalystReportsSection·styles) + 테스트·각 CLAUDE | 사용자: 증권사 종목분석보고서 최근 3개 종합 10줄요약 |
| 2026-07-13 | **종목검색 자동완성 원복(항목6)**. UX 개편(우측 패널화)에서 `RightPanel`의 `TickerSearch`가 `isValidTicker`(6자리 코드)만 받아 **종목명으로 검색 불가**였음. 조사 결과 자동완성 **백엔드는 완비**(`GET /api/stocks/search`·`collectors/stock_master.py`·`api.js::searchStocks`)돼 있고 UI 패턴도 `StockReport.jsx`에 살아있어 → **백엔드 변경 0, 프론트만** 복원. `TickerSearch`에 디바운스 검색(180ms)·`.autocomplete` 드롭다운(name·`{ticker}·{market}`)·키보드(↑↓·Esc)·바깥클릭 닫힘·선택 로직(활성후보→코드직접[`isValidTicker` SSOT]→첫후보→후보없으면 안내만) 이식. 선택 시 `onSubmit(ticker, 종목명)` → `{kind:'stock_report', args:{ticker, stock_name}, valid:true}`(종목명이 패널 제목 반영). `.autocomplete*` CSS 재사용·`.right-panel__search-box`(relative 앵커)만 신규. 검색 실패 graceful(조용히·코드 직접입력 보존). 라이브 검증(`/api/stocks/search?q=삼성`→삼성전자 등·`q=하이닉스`→SK하이닉스 실후보 반환). 프론트 230 green(+3), build clean, 안전(백엔드 변경 0·매매 API 0·hex 0·가격방향색 오용 0) | frontend/(RightPanel·styles·RightPanel.test) | 사용자: 종목코드 입력란에 종목명 검색 원복 |
| 2026-07-13 | **종목리포트 관심종목 별 토글(항목7) — 5주 로드맵 후속 7항목 완결**. 종목 종합리포트(`PopupStockReport→StockReportView`) 화면에서 관심종목에 담을 방법이 없었음(별·버튼 부재) → **별 아이콘(☆/★) 토글**로 담고 **등록완료를 한눈에 식별**. 워치리스트 **백엔드·API는 완비**(`addWatchlist`·`removeWatchlist`·`fetchWatchlistMembership`·`addErrorMessage`)돼 있어 → **프론트만**. 신규 `WatchlistStar.jsx`(캡슐화 위젯, `StockReport.jsx` 토글 로직 이식): 마운트 시 멤버십 자체 조회(환각 차단)→★/☆, 클릭 토글(미등록→add·등록→remove), 409 상한은 `addErrorMessage` 회색 안내, `busy` 중복클릭 차단, **불량 ticker 렌더 안 함**(`isValidTicker` SSOT), `aria-pressed`·`aria-label`. `StockReportView` 헤더 종목명 옆(`.report__name-row`) 배치. **등록완료 식별 = 주황 소프트 채움 ★**(`--c-emph`, 강조/확인 역할 — 가격방향색·경보색 아님), 미등록 ☆=회색. **사용자 명시적 클릭만**(자동 매매 아님)·관심종목=조회/저장(주문 아님). 레거시 `StockReport.jsx`(미마운트 죽은 코드)는 손대지 않음. 프론트 236 green(+6, WatchlistStar 신규), build clean, 안전(백엔드 변경 0·매매 API 0·hex 0·가격방향색 오용 0) | frontend/(WatchlistStar[신규]·StockReportView·styles) + 테스트·frontend CLAUDE | 사용자: 종목보고서 내 관심종목 등록(별 아이콘·등록완료 식별) |
| 2026-07-13 | **프로덕션 재배포 + GitHub 기반 CI/CD 구축(WIF·main 자동배포)**. 항목1~7 이 main 에만 있고 프로덕션 미반영 → **Phase A**: `gcloud run deploy --source=.`(런북 명령) 로 재배포(무마이그레이션·`create_all` no-op·항목1~7 스키마 변경 0), 리비전 `00004` 100% 트래픽·라이브 스모크(SPA 200·regime[FRED]·stocks/search[삼성]·market-outlook 200·analyst-reports/summary 200). **Phase B**: `.github/workflows/ci-cd.yml`(신규) — `push:main`·`pull_request` 트리거, **backend**(setup-uv·py3.13·`uv sync --locked`·`uv run pytest -q`[pytest addopts `-m 'not live'` → hermetic·시크릿 불필요])·**frontend**(node20·`npm ci`·`npm test --run`·`build`) 테스트 → **deploy**(`needs:[backend,frontend]`·`if: main push && vars.WIF_PROVIDER!=''`[미설정 skip=녹색]·**WIF 키리스**[`google-github-actions/auth`]·런북과 동일 `gcloud run deploy`·`concurrency` 직렬화). **WIF/IAM 설정 스크립트**(배포 SA·풀·프로바이더[레포 한정 attribute-condition]·principalSet 바인딩·GitHub `vars` WIF_PROVIDER/DEPLOY_SA)는 `DEPLOY_GCP.md`에 문서화 — **사용자가 직접 실행**(IAM/SA 자동화 금지). 워크플로에 시크릿 값 0(Secret Manager `:latest` 참조만·`.gcloudignore` 시크릿 미업로드). 워크플로 커밋 시 CI 실행·deploy 는 WIF 미설정이라 skip(예상). YAML 유효성·안전 grep(시크릿 값 하드코딩 0) 확인 | .github/workflows/ci-cd.yml[신규]·DEPLOY_GCP.md·CLAUDE | 사용자: 프로덕션 재배포 후 GitHub 기반 CI/CD 구축 |
| 2026-07-13 | **시황 자동 최신화(시장 국면 패널 로드 시)**. 시장 국면 패널을 열면 `MarketOutlookSection`이 저장된(오래된) 시황을 그대로 보여줘 최신이 아닐 수 있었음 → **마운트 시 최신 저장 시황 `date`가 KST 오늘과 다르면(stale) 기존 SSE 수집을 자동 트리거**해 최신화. 사용자 결정: 트리거는 패널 로드 시(챗 텍스트 답변 아님·수집이 느려 챗 안 막힘). `lib/marketOutlook.js`(순수) `isOutlookStale(reports, today)`(빈 배열·최신 date≠today→stale)·`todayStampKST()`(`Intl` 서울 TZ "YY.MM.DD"·네이버 형식 일치). `MarketOutlookSection`: `load()`가 reports 반환→마운트 effect가 `maybeAutoFetch`→stale이고 **날짜별 가드**(`localStorage['mo_autofetch_date']!==today`) 미소진이면 가드 세팅 후 기존 `fetchNaver()`(SSE 진행바) 자동 호출 + "오늘 최신 시황 자동 확인 중" 안내. 가드로 패널 반복 오픈·주말 무자료 시 재수집 폭주 방지, 마운트당 1회(`autoTriedRef`). **백엔드 변경 0**(기존 조회·SSE 수집 재사용)·매매 API 0·시황=리포트 인용·면책 불변. 프론트 244 green(+8, lib 5·component 3), build clean. 테스트: jsdom `localStorage` 전역 부재라 인메모리 `vi.stubGlobal`, 기존 테스트는 beforeEach 가드로 자동수집 억제 | frontend/(lib/marketOutlook·MarketOutlookSection) + 테스트·frontend CLAUDE | 사용자: 국면/시황 조회 시 DB 최신 시황이 현재일과 다르면 최신 수집 |
| 2026-07-17 | **종목 기술적 분석 — 고지로 이동평균선 대순환 통합**. 종목 상세 기술적 분석이 RSI·MA20-gap 점(点) 정보뿐이라 "추세 국면"이 없었음 → 고지로(小次郎講師) **이동평균선 대순환**(3 SMA 배열 6단계 사이클) 하단 통합. ①**엔진**(결정적·LLM 미개입): `stock/summary.py::_ma_grand_cycle`(`_stage_of` 6순열 분류·동률/결측 None) — 단기5/중기20/장기40(사용자 확정 표준)·stage/stage_name/arrangement/phase·`band_width_pct`((단기−장기)/장기×100)·`band_direction`(전환창20봉 전 절대폭 대비 확대/축소/유지)·`bars_in_stage`·`prev_stage`. `len<40`→None graceful. `build_stock_summary` 반환에 `ma_grand_cycle` 키(9→10키). ②**SSOT**: `GRAND_CYCLE_MA_PERIODS/STAGES` + `INDICATOR_CONFIG.grand_cycle`(periods+6단계 카탈로그)로 프론트 전파(번들 자동, 라우트 무변경). ③**프론트**: `lib/grandCycle.js`(순수: 6-스텝·글리프·밴드·인사이트)·`GrandCyclePanel.jsx`(6단계 스텝·현재 주황 강조·밴드/지속 readout·인사이트·면책·null 보류)를 `StockReportView` 차트블록 아래 배선 + `KLineChartPanel` **3MA 오버레이**(calcParams 5/20/40·단기 주황/중기 회색/장기 남색)·`theme.js` emph 추가. **색: 주황 강조만**(방향은 글리프, 가격방향색/경보색 금지·hex 0). ④**안전**: 판정=코드·서술=프론트(방법론 인용)·면책 고정·매매 API 0·일봉 무캐시(결과도 매요청 재계산). 사용자 결정: 5/20/40·카드+3MA 오버레이(대순환 MACD 제외). TDD(Red→Green): 백엔드 802 + 프론트 301 green, build clean, 안전 grep 0. 라이브 실증(005930 stage4 역배열·band −14.7%·전환 3→4 정합) | stock/(summary·constants)·frontend/(lib/grandCycle·GrandCyclePanel·StockReportView·KLineChartPanel·theme·styles) + 테스트·stock/·frontend CLAUDE | 사용자: 기술적 분석에 고지로 이동평균선 대순환 심층 통합 |
| 2026-07-17 | **과제 제출용 실행 노트북 + Intent 강화(analyst_report 인텐트)**. 과제 제출을 위해 5요소(Intent Classification·Prompt Routing·RAG·Tool Calling·UI)를 설명+로컬 실행 데모로 보여주는 Jupyter 노트북 `notebooks/투자에이전트_실행노트북.ipynb` 신설(무의존 빌더로 조립 → `nbconvert --execute` 실키 실행으로 출력 채움·에러 0). **약화 지점 강화**: 인텐트 데이터셋에 리포트 질의가 0건이라 "삼성전자 리포트 확보해줘"가 `stock_analysis`로 뭉뚱그려지던 문제 → **7번째 라벨 `analyst_report` 신설**(`chat/intent.py` LABELS·`intent_gen` 브리프)·데이터셋 55개 보강(380→435행)·재학습(`intent_clf.joblib`). `test_labels_are_exactly_six`→`_seven` 갱신 + 리포트 분류·benign 비차단 테스트. 라우팅 불변(툴 선택은 LLM tool_choice)·위험 가드레일 불변. 실모델 스모크(리포트→analyst_report, 기타 6라벨·가드레일 정상). 백엔드 809 green·노트북 안전 grep(키/시크릿·주문 API 0) | chat/(intent·intent_gen·models)·data/intent_dataset·notebooks/ + test·CLAUDE | 사용자: 과제 제출용 실행 노트북 + 약화된 intent 강화 |
| 2026-07-17 | **챗 애널리스트 리포트 수집 툴 — "확보해줘" 대응(방안 2: 요청 시 수집 + 없으면 먼저 제안)**. 종목 자문 시 애널리스트 리포트가 없으면 "없다"고만 하고, "확보해줘"엔 **오답**(‘reports 폴더에 PDF 추가·재인덱스’ = RAG `search_report`로 오라우팅)을 내던 문제. 원인: 챗에 **네이버 애널리스트 리포트를 수집할 콘텐츠 툴이 아예 없어** 유일한 리포트 툴 `search_report`(업로드 PDF RAG)로 떨어짐. 사용자 결정=**방안 2**(자동수집 아님 — 수집이 10~50초라 모든 종목 질문마다 자동 트리거하면 챗이 멈춤·기존 "수집은 명시적·SSE" 설계와 충돌). ①신규 콘텐츠 툴 `fetch_analyst_reports(ticker)`(`chat/tools.py`) — `analyst_service.fetch_and_summarize_for_ticker`(기존) 재사용·`ANALYST_CHAT_FETCH_LIMIT=5` 지연 캡·`_looks_like_ticker`(6자리)로 불량 티커 시 수집 미호출·저장 요약을 '리포트에 따르면' 출처 귀속으로 되먹임·0건 graceful. `CONTENT_TOOLS`·`_TOOL_IMPL` 등록(`chat.py` 무변경 — 범용 CONTENT_TOOLS 분기). ②`build_prompt ⑧` 재작성 — 두 채널 구분(`search_report`=업로드 PDF / `fetch_analyst_reports`=네이버 수집), **리포트 없으면 목표주가 날조 금지·"지금 수집해 올까요?" 먼저 제안** → 동의/명시 시 수집, 네이버 리포트를 'PDF 폴더 재인덱스'로 오안내 금지. **안전**: 조회·수집만(매매 API 0)·출처 귀속·판정 금지·면책 불변·guardrail 선차단 불변. TDD(Red→Green): 백엔드 808 green(+6). 라이브 실증(에스피지 058610·LG이노텍 011070 각 5건 수집) + **브라우저 e2e**("011070 리포트 확보해줘"→5증권사 목표주가[대신 130·유안타 140·DS 130·iM 110·교보 130만원] 출처 귀속+면책, 구 오답 재현 안 됨) | chat/(tools·build_prompt) + 테스트·chat CLAUDE | 사용자: 종목 자문 시 리포트 미확보면 확보 요청에 대응(방안 2) |
| 2026-07-17 | **프론트 도커 의존성 이미지 반영(stale node_modules 볼륨 제거)**. 로컬 도커 프론트가 `frontend-node-modules` **named volume**으로 node_modules를 보존했는데, named volume은 최초 1회만 이미지에서 채워져 **의존성 추가 후 이미지를 재빌드해도 반영 안 됨**(react-markdown 추가 시 컨테이너에 미설치 → 앱 전체 다운 → `docker compose exec frontend npm install` 수동 필요). 근본 해결: **node_modules를 바인드마운트 한 단계 위(`/node_modules`)에 굽고**(`frontend/Dockerfile` `npm ci && mv node_modules /node_modules`), compose는 `/app/node_modules`에 **빈 익명 볼륨**만 씌워 호스트 mac node_modules를 가림 → Node/Vite가 상위 `/node_modules`(이미지) 해석. stale 볼륨이 원천적으로 없어 **재빌드마다 자동 최신·컨테이너 내 재설치 영영 불필요**. `frontend/.dockerignore`가 node_modules 제외(COPY 유출 없음)·프로덕션(루트 Dockerfile 멀티스테이지) 무영향·백엔드 `backend-venv`는 동일 패턴 유지(스코프 밖). 실증: 재빌드 후 `npm install` 없이 `require.resolve('react-markdown')`→`/node_modules/...` OK, Vite 287ms ready·에러 0, 브라우저 앱 정상 + 대순환 카드 회귀 없음 | frontend/Dockerfile·docker-compose.yml·DOCKER.md | 사용자: 도커 컨테이너 재설치 없이 프론트 이미지에 의존성 반영 |
| 2026-07-15 | **톱바 연세대 소속 헤드라인 + 자체 제작 CI 엠블럼**. 상단 톱바에 과제 소속 표기 추가 — 좌측 DK 브랜드 옆(사용자 선택)에 세로 구분선 + **연세 엠블럼**(`YonseiEmblem` 자체 제작 인라인 SVG — 연세 블루 원형 배지+"연세", **공식 상표 CI 복제 아님**·과제 소속용 원본 마크·`DkMonogram` 동일 방식, 공식 파일 생기면 컴포넌트만 교체) + "연세대학교 AI핀테크"/"[AI핀테크Agent분석과 설계]" 2줄. 신규 토큰 **`--c-yonsei`**(theme.css SSOT·연세 블루 #003876·**파랑/남색 계열 브랜딩 전용, 가격 방향색 아님**). DK 브랜드+소속을 `.app__brand-group` 한 덩어리로 좌측 고정, 상태 칩 우측 유지. **백엔드 변경 0·프론트만**·매매 API 0·하드코딩 hex 0(theme.css 외)·방향색 오용 0. TDD(App.test 헤드라인 문구+엠블럼 aria 라벨). 프론트 245 green(+1), build clean. 사용자 결정: 자체 SVG 엠블럼(공식 로고 저작권 회피)·좌측 배치 | frontend/(App·theme.css·styles) + App.test·frontend CLAUDE | 사용자: 상단 헤드라인 "연세대학교 AI핀테크 [AI핀테크Agent분석과 설계]" + 연세 CI |
| 2026-07-15 | **목표가 매수/매도 분리 저장·추적 + 에이전트 목표가 추천**. 단일 `target_price`(이미 매수 관점)를 **매수 유지 + 매도 `sell_target_price` 추가**(하위호환 additive·무손실). 백엔드: 모델/스키마 `sell_target_price`(ge=0), **경량 idempotent 마이그레이션**(`infra/db::_run_lightweight_migrations` — `create_all` 이 못 붙이는 기존 테이블 `ADD COLUMN`, SQLite/Postgres 공용·`_ADDITIVE_COLUMNS` SSOT), `service._target_status(side)` 거울(매도 `current≥target`=도달·`≥target*(1-thr%)`=near) + enrich 매도 3키, store `update_targets(sentinel _UNSET 부분갱신)`(구 `update_target` 위임 유지), API POST/PATCH `sell_target_price`(PATCH `model_fields_set` 부분갱신·매도만 보내면 매수 불변). 챗: `manage_watchlist` set_target 에 `sell_target_price`(최소 1개), `build_prompt` **⑨ 목표가 추천 블록**(애널리스트 목표주가 우선→52주→PER 근거 **매수/매도 참고 범위**·단정 금지·출처 귀속·면책·`set_target` 확인 반영), `view_context` 매수/매도 인용 + `_stock_context` 52주 고/저 원값. 프론트: `TargetCell` **매수/매도 2행**(`TargetRow`·side 라벨·게이지·인라인 편집), `updateWatchlistTarget(ticker, {target_price?|sell_target_price?})` 부분 PATCH, `detectTargetAlerts` **이중상태 `{ticker:{buy,sell}}`+side**·`cmpNearTarget` 매수/매도 최소거리, `alertMessage` side 라벨, `popupRouter` set_target 최소1 검증, `ManageWatchlistConfirm` 매수/매도 표시·확인. **안전**: 매매 주문 API 0(저장·조회만)·추천=참고 범위·판정은 코드(reached/near/far)·자동실행 없음·색은 라벨 구분(방향색·경보색·hex 하드코딩 0)·guardrail 2층 불변. TDD(매도 상태 거울·부분갱신·마이그레이션 idempotency·이중전이 알림·popupRouter·ManageWatchlistConfirm·2행 렌더). 적대적 다각검증(안전·계약·정확성·테스트 4렌즈). 백엔드 761 + 프론트 257 green, build clean | watchlist/(db_models·models·store·sql_store·service)·api/watchlist·infra/db·chat/(tools·build_prompt·view_context)·frontend/(api·WatchlistView·watchlistLogic·App·popupRouter·ManageWatchlistConfirm·styles) + 테스트·각 CLAUDE | 사용자: 목표가 매수/매도 분리 저장·추적 + 에이전트 목표가 추천 |
| 2026-07-15 | **연세대 CI를 공식 CI 이미지로 교체**. 톱바 소속 마크의 자체 제작 SVG 엠블럼(`YonseiEmblem`)을 **사용자 제공 연세대학교 공식 CI 이미지**로 교체(`frontend/src/assets/yonsei-ci.png`, 228×97). `YonseiEmblem` = `<img class="app__yonsei" alt="연세대학교 로고">`(Vite 에셋 import·빌드 시 해시), `.app__yonsei` 높이 34px 고정·비율 유지(DK 모노그램 정렬). 대학명 텍스트는 `--c-yonsei` 유지(엠블럼은 이미지라 토큰 무관). App.test 는 `getByAltText`로 전환. 프론트 257 green, build clean(에셋 emit 확인) | frontend/(App·styles·assets/yonsei-ci.png) + App.test·frontend CLAUDE | 사용자: 연세대 CI를 이 파일로 변경 |
| 2026-07-15 | **톱바 헤드라인 "연세대학교"→"정보대학원"**. 공식 CI 이미지에 이미 "연세대학교" 워드마크가 있어 헤드라인 대학명 중복 → 대학명을 소속 대학원명으로 교체("정보대학원 AI핀테크"). App.test 단정 동기화. 프론트 green, build clean | frontend/App + App.test·frontend CLAUDE | 사용자: 연세대학교→정보대학원 |
| 2026-07-15 | **관심종목/잔고 종목 클릭 → 종목 상세 이동 + 잔고 미니차트**. ①**종목 클릭 네비게이션**(프론트): `RightPanelBody` 가 `openStock(ticker,name)=onSelect({kind:'stock_report',...})`(TickerSearch spec SSOT 재사용) → `PopupWatchlist`·`BalancePanel` 에 `onOpenStock` 관통. 관심종목은 **`.wl__row-top`(정보 영역)만** 클릭(row-bottom 목표가 편집·제거와 분리해 충돌 회피), 잔고는 `<tr>` 클릭. role/tabIndex/Enter·Space/hover·옵셔널. ②**잔고 미니차트**(백엔드+프론트): `watchlist.service._fetch_sparks_parallel`→**public `fetch_sparks_parallel`**(공용·import 사이클 없음), `api/balance.py` 가 `inquire_balance` 후 holdings 티커로 병렬 일봉 조회→각 holding 에 `spark` 후처리(graceful·`partial_failure` 미오염·현재가 캐시 금지). 프론트 `Sparkline` 을 **`components/Sparkline.jsx` 공용 컴포넌트로 추출**(`dir` 미지정 시 스파크 추세로 자동색; WatchlistView 는 dir 전달로 동작 불변), `BalancePanel` 보유종목 표 우측 "추세" 열. **안전**: 매매 주문 API 0(조회·표시 전환만)·색 토큰만(방향색/경보색 오용·hex 0)·클릭은 표시 전환만. TDD(balance spark 포함·graceful·Sparkline 렌더/자동색/null·row-top 클릭+편집버튼 미발화·tr 클릭·RightPanel onOpenStock→spec). 적대적 다각검증(계약·정확성·안전·테스트 4렌즈). 백엔드 769 + 프론트 269 green, build clean | api/balance·watchlist/service·frontend/(Sparkline[신규]·BalancePanel·WatchlistView·RightPanel·PopupWatchlist·api·styles) + 테스트·각 CLAUDE | 사용자: 종목 클릭 시 상세 이동 + 잔고에 관심종목 미니차트 |
| 2026-07-16 | **로그인 화면 CI 통일(주황 마름모 추가)**. 로그인 로고가 톱바와 달라 주황 마름모가 없었음(로그인=`.login__logo` 텍스트 배지, 톱바=`DkMonogram` SVG). `App.jsx`의 로컬 `DkMonogram`을 **`components/DkMonogram.jsx` 공용 컴포넌트로 추출**(`size`/`className` props, 색 토큰만·주황 마름모 `--c-emph` 포함) → 톱바(size 34)·로그인(size 40)이 공유해 CI 일관. 구 `.login__logo` CSS 폐기. TDD(LoginScreen 로고 SVG+주황 마름모 rect 단정). 프론트 270 green, build clean·hex 0 | frontend/(DkMonogram[신규]·App·LoginScreen·styles) + LoginScreen.test·frontend CLAUDE | 사용자: 로그인 화면 CI에도 주황 마름모 |
| 2026-07-16 | **시장 국면 대시보드 강화 — 판정근거 지표 카드 + 1년 월단위 히스토리 + 디자인**. 대시보드가 국면명·2×2·현금비중만 보여줘 판정에 쓰인 **지표 수치가 안 보이던** 문제. ①**판정근거 카드**: 엔진 `_INDICATOR_SPEC`+`classify_indicator`(값→구간 양호/중립/악화·탐욕/중립/공포, **score_axes 부등호와 1:1 SSOT·테스트로 잠금**)+`regime_breakdown`+`indicator_meta`, `/api/macro/regime` 이 `indicator_breakdown` 전개. 프론트 `RegimeGauge` 가 구 기여지표 리스트를 **`MacroIndicatorCards`**(축 그룹·값+구간 배지)로 교체. ②**1년 월단위 히스토리**: `fred.fetch_fred_series_history`(월 다운샘플)·`fear_greed.fetch_fear_greed_history`(CNN graphdata best-effort·월별 마지막값·graceful), `GET /api/macro/indicators/{key}/history`(캐시 `macro:history:` TTL 1일·available만 저장·불가/실패 available:false+note·항상 200·unknown key 400). 카드 클릭 → `MacroIndicatorHistoryOverlay`(오버레이 패턴 재사용) + **`MacroLineChart`**(순수 SVG·임계 가이드라인). ③디자인: 국면 결론→판정근거 카드(수치+구간)→시황 흐름. **안전**: 매매 API 0·국면 판정은 코드(수치/히스토리는 원천 표면화)·현재값 무캐시(원칙1) 유지·히스토리만 캐시(확정 과거)·색 토큰만(구간=주황 강조/회색, 방향색·경보색 오용 0·hex 0). TDD + 적대적 다각검증(4렌즈). 백엔드 787 + 프론트 280 green, build clean | macro/engine·api/main·collectors/(fred·fear_greed)·cache/keys·frontend/(RegimeGauge·MacroIndicatorCards·MacroIndicatorHistoryOverlay·MacroLineChart[신규]·api·styles) + 테스트·각 CLAUDE | 사용자: 국면 판정요소 수치 카드 + 클릭 시 1년 월단위 + 디자인 개선 |
| 2026-07-16 | **챗 봇 응답 마크다운 렌더**. 챗봇 응답이 `**굵게**`·`- 목록`·`## 제목` 등 마크다운 문법을 **문자 그대로** 보여줬음(`ChatMessage`가 원문 텍스트를 pre-wrap 렌더). **`react-markdown`+`remark-gfm`** 추가(사용자 결정)해 **봇 응답만** 마크다운 렌더(굵게/기울임/코드/코드블록/목록/제목/링크/인용/구분선/**GFM 표**), `.chat__md` 스타일(theme.css 토큰만). **사용자 입력은 평문 유지**(친 문자 그대로). **안전**: react-markdown 기본이 원문 HTML 미렌더(escape)→LLM 출력 `<script>` 등 **XSS 불가**(`dangerouslySetInnerHTML`·`rehype-raw` 없음), 링크 `target=_blank rel=noopener`. **스트리밍 호환**(토큰 누적 재파싱·커서/단계 로직 불변). TDD(마크다운 요소·GFM 표·사용자 평문·XSS 미렌더·커서). 프론트 286 green(+5), build clean(react-markdown 번들 증가로 chunk 경고만·에러 아님·hex 0) | frontend/(ChatMessage·styles·package[react-markdown·remark-gfm]) + ChatMessage.test·frontend CLAUDE | 사용자: 채팅창 마크다운 적용 |
| 2026-07-19 | **종목 차트 강화 — 일봉/주봉 × 3개월/1년/3년/10년 선택형**. 차트가 고정 일봉·6개월뿐이라 주기·기간 탐색이 불가했음 → **주기(일봉/주봉)×기간(3개월/1년/3년/10년) 모든 조합 선택**. **탐색(3-agent)**: KIS `FHKST03010100` 는 `FID_PERIOD_DIV_CODE` 로 D/W/M/Y 이미 지원(API 변경 0)·**~100/콜 상한**은 연속토큰 없어 페이지네이션 필요. **백엔드**: `collectors/kis/chart.fetch_chart_series`(신규·**date-window 후진 페이지네이션**·병합/중복제거/정렬·무진행·max_pages/max_bars 캡·period 공용) + `stock/summary.stage_segments_for_chart`(공용·표시 시계열로 리본 재계산·`_grand_cycle_segments` SSOT 재사용) + `GET /api/detail/{ticker}/chart?period=&range=`(화이트리스트 폴백·`_CHART_RANGE_DAYS`·`_resolve_client`·일봉 무캐시·불량ticker 400·KIS 실패 항상 200 graceful). 번들 `_fetch_chart`(요약용 6개월 단일호출)·`_sorted_closes`(→`_sorted_dated_closes` 파생) **행동 보존**. **프론트**: `ChartSection.jsx`(신규·세그먼트 선택기 네이비 active + `useFetch` 스왑 + **번들 차트 fallback**[초기/재조회/실패 시]) + `api.fetchStockChart` + `StockReportView` 가 KLineChartPanel 직접렌더→ChartSection 경유(칩·GrandCyclePanel 불변) + styles. **⚠ pin**: 정량 요약(RSI/MA/52주/현재 대순환 단계·GrandCyclePanel)은 **번들[일봉]에 고정** — 차트 주기/기간 선택이 정량 판정을 바꾸지 않는다(라우트도 표시 데이터만). **리본은 표시 시계열로 재계산**(주봉=주봉 대순환·10년=10년 단계 이력). **안전**: 매매 API 0·KIS 조회만·대순환 판정=코드(LLM 0)·차트 무캐시·색 토큰만(선택기 네이비·방향색/경보색 0·hex 0). TDD. **적대적 4렌즈 검증(페이지네이션·계약·안전+pin·행동보존) → 무결함(0건)**. 백엔드 871 + 프론트 350 green, build clean, **라이브 실증**(005930: D/3m 60·D/1y 243[3콜]·D/3y 728[8콜]·W/10y 521[6콜]·**D/10y 2452[~25콜·2.5s]**·현재단계 일봉 4[역배열]/주봉 1 정합) | collectors/kis/chart·stock/summary·api/detail·frontend/(ChartSection·StockReportView·api·styles) + 테스트·각 CLAUDE | 사용자: 일봉+주봉·3개월/1년/3년/10년 모든 조합 선택 |
| 2026-07-19 | **종목 차트 대순환 스테이지 리본 오버레이 — 캔들 하단 시간축 단계 표시**. 종목 상세 기술적 분석 차트가 '현재' 대순환 단계만(GrandCyclePanel 카드) 보여줘 "언제 어느 단계였는가"가 차트에 없었음 → **캔들 하단 얇은 리본**으로 최근 6개월 단계 이동 표시. (**5/20/40 MA 는 이미 렌더 중**이라 변경 없음 — 3-agent 정독으로 확인.) 사용자 결정: 형태=하단 스테이지 리본. **반영 근거**: `_stage_of/_stage_at`(순수·결정적, LLM 0)이 봉별 단계를 이미 분류(내부 계산만·미노출). **백엔드**: `_sorted_dated_closes`(신규·`_sorted_closes` 파생)·`_grand_cycle_segments`(순수 — 연속 동일 단계 런을 `{stage,start_date,end_date}` **날짜 키** 구간으로, None[봉<40·동률]은 런 분리)·`_ma_grand_cycle(closes, dates=None)` 에 `stage_segments` 추가(하위호환 [])·`build_stock_summary` 배선(**최상위 10키 계약 불변** — ma_grand_cycle 하위 키). **프론트**: `lib/grandCycle.ribbonSegments`(순수·카탈로그 SSOT 라벨/글리프·isCurrent=마지막 구간)·`lib/theme.readChartPalette` 리본 뉴트럴 톤 5토큰·`KLineChartPanel` **klinecharts 커스텀 오버레이**(`registerOverlay({name:'gcStageBand',createPointFigures})` idempotent + 세그먼트별 `createOverlay(points:[{timestamp}])`, 하단 rect+번호+글리프·화면밖/폭<1 클램프·폭≥16만 라벨·graceful skip)·`StockReportView` props 배선(`stageSegments`/`currentStage`)·하단 범례. **색: 뉴트럴 국면 톤(상승/전환/하락)·현재 주황**, 방향은 **글리프(▲▼◆)**(가격 방향색·경보색·hex 0). **안전**: 판정=코드·LLM 0·차트 무캐시(세그먼트도 매요청 재계산)·매매 API 0·면책(GrandCyclePanel) 불변. TDD. **적대적 4렌즈 검증(정확성·계약·안전·klinecharts API[최고위험·브라우저 미검증]) → 무결함(0건)**. 백엔드 860 + 프론트 345 green, build clean, **라이브 실증**(005930: 6구간 stage1 장기상승→전환 진동→stage3→**stage4 현재**[역배열]·마지막 세그먼트=현재 단계 정합·시간 오름차순) | stock/(summary·CLAUDE)·frontend/(lib/grandCycle·lib/theme·KLineChartPanel·StockReportView·styles·CLAUDE) + 테스트 | 사용자: 차트에 5/20/40 MA + 스테이지 구분 오버레이 |
| 2026-07-19 | **국면 이동 궤적(족적) 시각화 — 경기×심리 매트릭스 위 3년 트레일**. 시장 국면 대시보드가 '지금' 국면 한 점만 보여줘 이동 이력이 안 보였음 → 최근 N개월(12/24/36·기본 36) 국면을 **같은 매트릭스 위 족적**으로. 사용자 결정(AskUserQuestion): 형태=추세 궤적(trail)·기간=36 선택형·배치=시장 국면 인라인. **반영 가능 근거(3-agent 탐색)**: 판정 엔진이 순수·결정적(`judge_regime`) → 과거 지표 재현 가능 · 월별 히스토리 수집기(`fetch_fred_series_history`/`fetch_fear_greed_history`) 기존 · 게이지 좌표 변환 기존. **백엔드**: `macro/regime_history.build_trajectory`(순수 재현·월 union 정렬·**진행 중 당월 결정적 제외**[`exclude_month`, FRED `frequency=m` 부분월 평균이 확정 '현재' 점·캐시가 되는 아티팩트 방지]·**양축 결측 스킵**[경기·심리 둘 다 있어야 플롯]·공포탐욕 결측 시 심리축 VIX 단독)·`GET /api/macro/regime/history`(FRED3+공포탐욕 `fetch_parallel` 병렬·clamp 1..60·`macro:history:regime:` 캐시[available만·정합실패 미저장]·항상 200). **프론트**: `lib/regimeTrajectory`(순수 — **`regimeMarkerPos` 좌표 SSOT** 게이지와 공유·`RegimeGauge` 인라인 계산 이관·셀 겹침 **황금각 나선 오프셋**·`buildTrajectory` pathD/isTransition/opacity)·`RegimeTrajectory.jsx`(자체 완결 SVG 사분면+트레일+전환 화살표+현재 주황 라벨·기간 세그먼트 탭·graceful·면책)·`api.fetchRegimeTrajectory`·`RegimeGauge` 배선. **색: 과거 회색·현재 주황(--c-emph)만**(방향색/경보색/hex 0). **적대적 다각검증(정확성·계약·안전·UX) 워크플로우** → medium 발견(당월 부분데이터가 '현재' 점으로 캐시될 위험) 수정 + coverage 보강. TDD. 백엔드 855 + 프론트 338 green, build clean, 안전 grep 0, **라이브 실증**(36개월 2023~2026·역수익률곡선기[경기악화 지속]+저변동성[심리탐욕]→과열/수축 진동이 실제와 정합·당월 제외 확인) | macro/(regime_history·CLAUDE)·api/(main·CLAUDE)·cache/keys·frontend/(lib/regimeTrajectory·RegimeTrajectory·RegimeGauge·api·styles·CLAUDE) + 테스트 | 사용자: 최근 3년 국면 이동을 매트릭스에 족적으로 시각화 |
| 2026-07-18 | **권한 관리(RBAC) + 질문 사용량 한도(관리자 제어)**. 사용자 요청: ①계정 수정/삭제·이용 통계·토큰 한도 제어 관리자 기능, `dukkikim@yonsei.ac.kr` 관리자 승격 ②관리자 제외 계정 질문 소비 제어(~20회). 사용자 결정(AskUserQuestion): **질문 횟수 기반 + 매일(KST) 리셋**. **Phase1**(유저 모델·권한·시드): `User` 5컬럼 추가(`is_admin`·`daily_limit`=20·`used_today`·`usage_date`·`total_questions`)+`infra/db._ADDITIVE_COLUMNS` 백필, `auth/usage.py`(순수: `today_kst`·`effective_used`·`is_over_limit`[관리자 면제]·`consume`·`quota_snapshot`), `deps.get_admin_user`(403 게이트), `admin_seed.seed_admins`(startup·`ADMIN_EMAILS` 기본 dukkikim@yonsei.ac.kr 기존 유저 승격·idempotent), `_user_public`에 quota 노출. **Phase2**(챗 enforcement): `/api/chat`·`/stream` 진입부 `is_over_limit` 선차단(LLM 미호출·안내문만)+성공 턴 `consume`. **Phase3**(관리자 API `api/admin.py`): GET users(통계·해시/KIS원문 미노출)·PATCH(is_admin·daily_limit 부분갱신·음수422·자기 관리자해제 400)·POST reset-usage·DELETE(유저+스코프데이터[관심종목·대화·KIS] 정리·자기삭제 400). **Phase4**(프론트): `AdminPanel`(우측 '회원 관리' 탭·관리자만·카드로우·권한토글·한도편집·리셋·2단계삭제·자기가드) + 톱바 질문 잔량 칩(관리자 무제한/일반 N/M·소진 주황·챗 완료 시 fetchMe 갱신) + `api.js` 4함수. **안전**: 매매 API 0·판정/차단은 코드(LLM 미개입)·비번해시/KIS원문 미노출·색 토큰만(확인=주황·삭제 뉴트럴 2단계·경보색 미사용·hex 0). TDD(usage 순수·RBAC 시드·admin 라우트 10·챗 enforcement 3·AdminPanel 6·RightPanel 탭 2). 백엔드 839 + 프론트 320 green, build clean, **라이브 e2e**(재시작 seed 승격→관리자 무제한·멤버 403·목록·한도PATCH·reset·자기삭제400·멤버삭제 전부 통과) | auth/(models·usage·deps·admin_seed)·infra/(db·config)·api/(auth·admin·chat·main)·frontend/(AdminPanel·RightPanel·App·api·styles) + 테스트·각 CLAUDE | 사용자: 권한관리 + 토큰 사용량 한도 |
