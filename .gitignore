# 환경변수 / 시크릿
.env
.env.local

# 캐시 / 토큰 영속
.cache/
*.token.json

# 증권사 리포트 PDF(RAG 입력) — 사용자 데이터·저작물, 커밋 금지
reports/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
*.egg-info/

# uv
# (uv.lock은 커밋 대상 — 무시하지 않음)

# 프론트엔드 (Vite/React) + Node/Vite 부산물 (루트·frontend 등 모든 depth — 재발 방지)
node_modules/
.vite/
frontend/dist/

# IDE / OS
.idea/
.DS_Store
