# 🎬 데모 시나리오 (발표용)

디케이 투자에이전트 라이브 시연 대본. **7개 비트**로 LLM 앱 5요소(인텐트 분류·프롬프트 라우팅·RAG·툴 콜링·UI) + 안전 설계를 모두 보여준다. 각 비트는 "무엇을 입력 → 화면에 무엇이 → 어떤 기술을 시연" 구조.

> 근거: 이 대본의 핵심 동작(시황 최신화·표+플로우차트·잔고+파이차트 등)은 개발 세션에서 라이브로 검증됨. 실제 발표 전 **한 번 리허설** 권장.

---

## 0. 사전 준비 (리허설 필수)

| 항목 | 내용 |
|---|---|
| 로그인 | 데모 계정 미리 로그인. 관심종목 몇 개 등록·KIS 공유키 동작 확인 |
| **사전 수집** | **시황·애널리스트 리포트를 미리 1회 수집**해 둘 것 — 첫 수집은 10~50초라 라이브에서 느리다. 미리 해두면 즉답 |
| 화면 배치 | 좌측 = 상시 채팅 / 우측 = 동적 패널. **분할선 드래그로 채팅 폭을 넓혀두면** 표·다이어그램이 잘 보인다 |
| 네트워크 | 외부 API(KIS·FRED·OpenAI·네이버) 호출 — 안정적 회선 |
| 접속 | 로컬 `http://localhost:5173` 또는 프로덕션 `https://dk-invest-agent-816686454504.asia-northeast3.run.app` |

---

## 1. 데모 비트 (순서대로)

| # | 입력(질문) | 좌측 답변 | 우측 패널 | 시연 포인트 |
|---|---|---|---|---|
| **1** | **"지금 시장 어때?"** | 국면·권장 현금비중 설명 + **최신 시황 인용** (스트리밍에 "최신 시황 확인 중" 단계) | **시장 국면 대시보드** (경기×심리 2축 매트릭스·이동 궤적·판정근거 지표 카드) | 판정=**코드**(결정적 엔진)·LLM은 설명만 / 인텐트→패널 **결정적 라우팅** / 시황 **동기 최신화**(시스템일자 기준) |
| **2** | **"방어주와 성장주 차이랑, 투자 판단 흐름 알려줘"** | **비교 표(GFM) + mermaid 플로우차트 자동 생성** (그림 요청어 없이) + 용어 설명 + 면책 | (패널 없음 · 텍스트) | **답변 서식 기본 곁들임** / react-markdown + **mermaid** / 하이브리드 상위 모델(terra) |
| **3** | **"삼성전자 어때?"** | 종목 요약 서술 + 면책 | **종목 종합리포트** (정량요약 · PER vs 자기 과거평균 · RSI · **이동평균선 대순환 6단계** · 캔들차트) | **정량 판정=코드** / **function calling**(show_stock_report) / **환각 차단**(실데이터는 프론트 자체조회) |
| **4** | **"삼성전자 리포트 확보해줘"** → (제안이 뜨면) **"응 수집해줘"** | "확보된 리포트 없음 → 지금 수집할까요?" 제안 후 **네이버 증권사 리포트 수집·요약**(증권사별 목표주가) "리포트에 따르면 …" | 종목상세 애널리스트 섹션 갱신 | **콘텐츠 툴 = RAG 실질** / **출처 귀속** · 매수/매도 판정 금지 · 면책 |
| **5** | **"요즘 살만한 종목 추천해줘"** | "스크리너에 따르면 상승 배열(1·6단계)로 분류됩니다 …" (엔진 귀속 · 면책) | **후보 종목 패널** (대순환 단계 스캔) | **규칙 엔진 스크리너**(코드) / "매수 추천 아님" 안전 프레이밍 |
| **6** | (내 잔고 패널을 연 상태에서) **"내 포트폴리오 비중 조정 방향 알려줘. 그림으로도 보여줘"** | 잔고 스냅샷 근거 **집중도·비중·손익 상담** + **비중 파이차트(mermaid)** + 면책 | **잔고 패널** (순자산 · 평가손익 · 보유종목 · 스파크라인) | **현재 화면 컨텍스트 연계**(view_context 세션 핀) / **코드 근거 자문** / mermaid pie |
| **7** | **"빚내서 한 종목에 몰빵할까?"** | **즉시 위험 환기 + 분산 안내** (정상 답변이 아님) | (없음) | **안전 가드레일 1층** (결정적 정규식 하드블록 · **LLM 미호출**) · 매매 API 0 |

### 대비 시연 (안전의 "균형" 강조)
비트 7 **직후** → **"내 잔고에서 손실 난 종목 알려줘"** 를 던진다 → **정상 답변**.
- 메시지: "위험 요청은 막되, 정당한 조회·상담은 통과한다."
- 기술: **2층 방어** — ① 결정적 정규식 하드블록(비트 7) + ② ML→LLM 재분류로 오탐 구제(이 대비 질문). 정당한 잔고 조회가 예전엔 오차단됐던 문제를 이 구조로 해소.

---

## 2. LLM 앱 5요소 ↔ 데모 비트 매핑 (과제 평가 관점)

| LLM 앱 요소 | 구현 | 시연 비트 |
|---|---|---|
| ① Intent Classification | scikit-learn TF-IDF(char_wb) + LogisticRegression **7분류** + 결정적 가드레일 | 1·3·5·7 (질문마다 다른 패널로 라우팅) |
| ② Prompt Routing | 인텐트→네비 패널 결정적 매핑 + 시스템 프롬프트 자동 조립(기준표 SSOT) | 1·3·5 |
| ③ RAG | (i) PDF 임베딩(pdfplumber→numpy 코사인) (ii) 콘텐츠 툴이 곧 검색증강생성 | 4 (애널리스트 수집) |
| ④ Tool Calling | OpenAI function calling — 표시 툴(프론트 조회) / 콘텐츠 툴(서버 실행 되먹임) | 3·4·5·6 |
| ⑤ UI | React + Vite 좌 채팅 + 우 동적 패널 · 마크다운/표/**mermaid** 렌더 | 전 비트 (특히 2·6) |
| ★ 안전 설계 | 매매 API 0 · 판정=코드 · 2층 가드레일 · 출처 귀속·면책 | 7 + 대비 시연 |

---

## 3. 스토리라인 (멘트 흐름)

1. **취지** — "매매는 하지 않고 판단을 돕는다. **판정은 코드, 설명은 LLM.**" (비트 1·3에서 자연스레 강조)
2. **UX** — 좌측 대화 + 우측 실시간 패널, 요청 없이도 **표·그림 기본 곁들임** (비트 1·2)
3. **정확성·안전** — 결정적 엔진 · 환각 차단 · **2층 가드레일** (비트 3·7)
4. **확장(RAG·발굴)** — 리포트 수집 · 후보 스크리너 · 포트폴리오 상담 (비트 4·5·6)
5. **마무리** — 계획(AWS·gpt-4o) → 구현(GCP·하이브리드) **진화** + 향후 개선안 (`README.md` 발표 자료 참조)

---

## 4. 리스크 · 폴백

- **수집 지연(비트 4)**: 미리 수집해두거나 이미 수집된 종목으로 대체. 라이브 수집 시연 시 "수집 중" 진행바(SSE)를 오히려 어필 포인트로.
- **LLM 변동성**: 표·mermaid는 "기본 곁들임"이라 대부분 나오지만 100%는 아니다 — 비트 2는 리허설로 확인된 프롬프트를 쓴다.
- **장중/장마감 무관**: 시세·잔고는 실시간 조회라 값은 항상 나온다(현재가 무캐시). 국면·현금비중도 결정적으로 산출.
- **비트 5의 이중 실행**: 스크리너는 백엔드 되먹임 + 프론트 패널이 각각 조회(기본 필터 동일이라 후보 일치). 텍스트가 authoritative.

---

## 5. (선택) 추가 시연 카드

시간 여유가 있으면:
- **YouTube 요약** — "이 영상 요약해줘 + URL" → 콘텐츠 툴이 자막 가져와 요약(출처 귀속). ※프로덕션(데이터센터 IP)에서는 YouTube 차단으로 실패할 수 있음 → 로컬 시연 권장.
- **회원·관리자** — 관리자 계정으로 '회원 관리' 탭(권한·질문 한도) + 톱바 질문 잔량 칩.
- **국면 이동 궤적** — 시장 국면 대시보드에서 최근 12/24/36개월 궤적(엔진이 과거 지표를 결정적으로 재현).
